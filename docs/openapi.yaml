openapi: 3.1.0
info:
  title: shopnxebk REST API
  version: 1.0.0
  description: Authentication, Store management, Theme marketplace, Platform settings, Plans & Pricing, Customer profiles/groups/credits/eligibility, Brand, Product, composed Product Detail, multilingual Product Option/Variant, typed Custom Field, Product Image, reusable Media, and Fulfillment Type REST endpoints. Categories and Product Types remain GraphQL-only as standalone resources and are documented by the Lighthouse schema; the Product Detail façade exposes bounded selector projections for Store Admin composition.
servers:
  - url: http://localhost:8000
tags:
  - name: Platform Accounts
    description: Platform staff and merchant provisioning for the separate admin frontend.
  - name: Platform Stores
    description: Searchable and paginated Store catalog administration for Platform staff.
  - name: Store Users
    description: Member and role management inside one selected Store.
  - name: Platform Settings
    description: Global SaaS language, currency, and fulfillment configuration for the /admin/settings component.
  - name: Themes
    description: Platform marketplace/releases/licenses and Store-installed Theme copies.
  - name: Store Policies
    description: Platform policy-type catalog, Store-local translations/version history, and published storefront reads.
  - name: Store Pages
    description: Store Admin hierarchy, lifecycle, configuration, and multilingual Page content.
  - name: Customers
    description: Store-scoped buyer profiles, multilingual group display names, append-only credit ledger, Category access, and targeted group discounts.
  - name: Catalog Brands
    description: Store-scoped Brand identity, localized content, official website, and origin management.
  - name: Catalog Collections
    description: Store-scoped merchandising collections, localized content, rules, memberships, refreshes, and AI audit history.
  - name: Catalog Products
    description: Store-scoped Product lifecycle, localization, classification, pricing, inventory, and commerce metadata.
  - name: Catalog Custom Fields
    description: Store-scoped typed field definitions, select options, and Product or Variant values.
  - name: Store Media
    description: Reusable Store-owned uploads, processing, authenticated delivery, and Product or Variant attachment.
  - name: Translations
    description: Tenant-isolated status for durable after-commit automatic translation work.
paths:
  /api/health/live:
    get:
      summary: Liveness probe
      responses:
        '200':
          description: Process is responding
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Health'}
  /api/health/ready:
    get:
      summary: Readiness probe
      responses:
        '200': {description: Dependencies are ready}
        '503': {description: A required dependency is unavailable}
  /api/v1/auth/register:
    post:
      summary: Register a user and first store
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/RegisterRequest'}
      responses:
        '201':
          description: User and complete draft Store created
          content:
            application/json:
              schema:
                type: object
                required: [user, store, dashboard_url]
                properties:
                  user: {$ref: '#/components/schemas/User'}
                  store: {$ref: '#/components/schemas/Store'}
                  dashboard_url: {type: string, format: uri, description: Store Admin dashboard with the Store public ULID in the store query parameter.}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/auth/login:
    post:
      summary: Start a stateful session
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email: {type: string, format: email}
                password: {type: string, format: password}
      responses:
        '200': {description: Session established}
        '422': {$ref: '#/components/responses/ValidationError'}
        '429': {description: Rate limited}
  /api/v1/auth/token:
    post:
      summary: Issue a store-scoped bearer token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password, device_name, store_id]
              properties:
                email: {type: string, format: email}
                password: {type: string, format: password}
                device_name: {type: string, maxLength: 255}
                store_id: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$', description: Public Store ULID}
      responses:
        '200': {description: Plain Store-bound token returned once; expires after 30 days by default}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '429': {description: Rate limited}
  /api/v1/auth/me:
    get:
      security: [{sanctum: []}]
      summary: Current user
      responses:
        '200':
          description: User resource including exclusive account scope
          content:
            application/json:
              schema:
                type: object
                required: [user]
                properties:
                  user: {$ref: '#/components/schemas/User'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
  /api/v1/auth/session:
    get:
      security: [{sanctum: []}]
      summary: Bootstrap the authenticated dashboard session
      description: Returns the current User and exclusive Platform Admin/Store Admin interface profile atomically.
      responses:
        '200':
          description: Current User and scoped interface access profile
          content:
            application/json:
              schema: {$ref: '#/components/schemas/DashboardSessionResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
  /api/v1/auth/interfaces:
    get:
      security: [{sanctum: []}]
      summary: Resolve Platform Admin and Store Admin interfaces
      description: Returns stable Platform and Store keys, but exclusive user scope permits only one interface to be available.
      responses:
        '200':
          description: Scoped interface access profile
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AccessInterfacesResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
  /api/v1/auth/logout:
    post:
      security: [{sanctum: []}]
      summary: Revoke the current session or token
      responses:
        '200': {description: Logged out}
        '401': {$ref: '#/components/responses/Unauthenticated'}
  /api/v1/auth/password:
    patch:
      security: [{sanctum: []}]
      summary: Change the signed-in user's password
      description: Available to both Platform and Store users. Verifies the current password, keeps the active browser session signed in, rotates session/remember credentials, revokes personal access tokens, and invalidates outstanding MFA challenges.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [current_password, password, password_confirmation]
              properties:
                current_password: {type: string, format: password}
                password: {type: string, format: password, minLength: 12, description: Must contain uppercase, lowercase, a number, and a symbol and differ from current_password.}
                password_confirmation: {type: string, format: password}
      responses:
        '200':
          description: Password changed and personal API tokens revoked
          content:
            application/json:
              schema:
                type: object
                required: [message, tokens_revoked]
                properties:
                  message: {type: string, example: Password changed successfully.}
                  tokens_revoked: {type: integer, minimum: 0}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '419': {description: CSRF token mismatch or expired session}
        '422': {$ref: '#/components/responses/ValidationError'}
        '429': {description: Rate limited to five attempts per minute per authenticated user}
  /api/v1/auth/stores:
    get:
      security: [{sanctum: []}]
      summary: List active memberships
      responses:
        '200':
          description: Public-safe Store resources
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    type: array
                    items: {$ref: '#/components/schemas/Store'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {description: Platform account cannot list Stores}
  /api/v1/auth/tokens:
    get:
      security: [{sanctum: []}]
      summary: List the current account's personal access tokens
      parameters:
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200':
          description: Paginated personal access tokens
          content:
            application/json:
              schema:
                type: object
                required: [data, links, meta]
                properties:
                  data: {type: array, items: {$ref: '#/components/schemas/PersonalAccessToken'}}
                  links: {$ref: '#/components/schemas/PaginationLinks'}
                  meta: {$ref: '#/components/schemas/PaginationMeta'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
  /api/v1/auth/forgot-password:
    post:
      summary: Queue a password reset notification
      responses:
        '202': {description: Generic response}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/auth/reset-password:
    post:
      summary: Reset a password with a broker token
      responses:
        '200': {description: Password reset}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/auth/verify-email/{id}/{hash}:
    get:
      summary: Verify a signed email link
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}}
        - {name: hash, in: path, required: true, schema: {type: string}}
      responses:
        '200': {description: Email verified}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/platform/roles:
    get:
      tags: [Platform Accounts]
      security: [{sanctum: []}]
      summary: List assignable Platform roles
      description: Requires Platform scope and manage platform users.
      responses:
        '200':
          description: Platform role names
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RoleListResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/platform/users:
    get:
      tags: [Platform Accounts]
      security: [{sanctum: []}]
      summary: List Platform users
      description: Paginated; requires Platform scope and manage platform users.
      parameters:
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200':
          description: Platform users and Platform roles
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ManagedUserListResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [Platform Accounts]
      security: [{sanctum: []}]
      summary: Create a Platform user
      description: Creates only a Platform-scoped identity and accepts only Platform roles.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/CreateManagedUserRequest'}
      responses:
        '201':
          description: Platform user created
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ManagedUserResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/users/{user}:
    get:
      tags: [Platform Accounts]
      security: [{sanctum: []}]
      summary: View a Platform user
      parameters:
        - {name: user, in: path, required: true, schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}}
      responses:
        '200':
          description: Platform user
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ManagedUserResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Platform user not found}
    patch:
      tags: [Platform Accounts]
      security: [{sanctum: []}]
      summary: Update a Platform user and Platform roles
      parameters:
        - {name: user, in: path, required: true, schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/UpdateManagedUserRequest'}
      responses:
        '200': {description: Platform user updated}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Platform user not found}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/merchant-roles:
    get:
      tags: [Platform Accounts]
      security: [{sanctum: []}]
      summary: List Store roles assignable during merchant provisioning
      description: Requires Platform scope and manage stores.
      responses:
        '200':
          description: Store role names
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RoleListResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/platform/merchants:
    get:
      tags: [Platform Accounts]
      security: [{sanctum: []}]
      summary: List merchants
      description: Paginated Stores and their Store-scoped users/roles; requires manage stores.
      parameters:
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200':
          description: Merchant list
          content:
            application/json:
              schema:
                type: object
                required: [data, links, meta]
                properties:
                  data: {type: array, items: {$ref: '#/components/schemas/Merchant'}}
                  links: {$ref: '#/components/schemas/PaginationLinks'}
                  meta: {$ref: '#/components/schemas/PaginationMeta'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [Platform Accounts]
      security: [{sanctum: []}]
      summary: Create a merchant
      description: Atomically creates a Store user, draft Store, settings, platform domain, Theme license, published installed Theme copy, active membership, and Store roles; Owner is always included.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/CreateMerchantRequest'}
      responses:
        '201':
          description: Merchant created
          content:
            application/json:
              schema:
                type: object
                required: [data, dashboard_url]
                properties:
                  data: {$ref: '#/components/schemas/Merchant'}
                  dashboard_url: {type: string, format: uri}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/merchants/{merchant}:
    get:
      tags: [Platform Accounts]
      security: [{sanctum: []}]
      summary: View a merchant by Store ULID
      parameters:
        - {name: merchant, in: path, required: true, schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}}
      responses:
        '200': {description: Merchant resource}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Merchant not found}
    patch:
      tags: [Platform Accounts]
      security: [{sanctum: []}]
      summary: Update a merchant owner and Store
      description: Existing Store roles are preserved. A changed owner email becomes unverified.
      parameters:
        - {name: merchant, in: path, required: true, schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/UpdateMerchantRequest'}
      responses:
        '200': {description: Merchant updated}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Merchant not found}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/stores:
    get:
      tags: [Platform Stores]
      security: [{sanctum: []}]
      summary: Search, filter, sort, and paginate Stores
      description: Requires Platform scope and manage stores. Never send X-Store-ID.
      parameters:
        - {name: search, in: query, schema: {type: string, maxLength: 120}, description: Case-insensitive Store name, legal name, slug, contact email, primary domain, or related Store-member name/email search.}
        - {name: status, in: query, schema: {type: string, enum: [draft, trial, active, suspended, frozen, closed]}}
        - {name: business_type, in: query, schema: {type: string, enum: [ecommerce, b2b, services, digital, restaurant, marketplace]}}
        - {name: currency_code, in: query, schema: {type: string, pattern: '^[A-Za-z]{3}$'}}
        - {name: language_code, in: query, schema: {type: string}}
        - {name: country_code, in: query, schema: {type: string, pattern: '^[A-Za-z]{2}$'}}
        - {name: is_verified, in: query, schema: {type: boolean}}
        - {name: is_ai_enabled, in: query, schema: {type: boolean}}
        - {name: is_pos_enabled, in: query, schema: {type: boolean}}
        - {name: is_b2b_enabled, in: query, schema: {type: boolean}}
        - {name: is_marketplace_enabled, in: query, schema: {type: boolean}}
        - {name: created_from, in: query, schema: {type: string, format: date}}
        - {name: created_to, in: query, schema: {type: string, format: date}}
        - {name: sort, in: query, schema: {type: string, enum: [name, slug, status, created_at, updated_at], default: created_at}}
        - {name: direction, in: query, schema: {type: string, enum: [asc, desc], default: desc}}
        - {name: page, in: query, schema: {type: integer, minimum: 1, default: 1}}
        - {name: per_page, in: query, schema: {type: integer, minimum: 1, maximum: 100, default: 10}}
      responses:
        '200':
          description: Paginated Store catalog
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PaginatedStoreResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
    post:
      tags: [Platform Stores]
      security: [{sanctum: []}]
      summary: Create an unassigned Store
      description: Requires manage stores. Creates normalized locale/settings and platform/custom domain records, but no owner, membership, role, plan, or subscription; status defaults to draft.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/CreatePlatformStoreRequest'}
      responses:
        '201':
          description: Store created
          content:
            application/json:
              schema: {$ref: '#/components/schemas/StoreResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/stores/{store}:
    parameters:
      - {name: store, in: path, required: true, schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}, description: Public Store ULID}
    get:
      tags: [Platform Stores]
      security: [{sanctum: []}]
      summary: View a Store
      description: Requires Platform scope and manage stores.
      responses:
        '200':
          description: Store resource
          content:
            application/json:
              schema: {$ref: '#/components/schemas/StoreResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Store not found}
    patch:
      tags: [Platform Stores]
      security: [{sanctum: []}]
      summary: Edit a Store
      description: Internal Billing links, raw JSON, preferences, owner data, and roles are prohibited.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/UpdatePlatformStoreRequest'}
      responses:
        '200':
          description: Store updated
          content:
            application/json:
              schema: {$ref: '#/components/schemas/StoreResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Store not found}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/stores/{store}/locale-settings:
    parameters:
      - {name: store, in: path, required: true, schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}, description: Public Store ULID}
    get:
      tags: [Platform Stores]
      security: [{sanctum: []}]
      summary: View Store locale settings
      description: Requires Platform scope and manage stores. UTF-8 and automatic IANA timezone daylight-saving behavior are read-only managed standards.
      responses:
        '200':
          description: Unified Store regional and display-format settings
          content:
            application/json:
              schema: {$ref: '#/components/schemas/StoreLocaleSettingsResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Store not found}
    patch:
      tags: [Platform Stores]
      security: [{sanctum: []}]
      summary: Update Store locale settings
      description: Updates Store currency/language/country/timezone and normalized display-format preferences in one transaction.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/UpdateStoreLocaleSettingsRequest'}
      responses:
        '200':
          description: Store locale settings updated
          content:
            application/json:
              schema: {$ref: '#/components/schemas/StoreLocaleSettingsResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Store not found}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/stores/{store}/domains:
    parameters:
      - {name: store, in: path, required: true, schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}, description: Public Store ULID}
    get:
      tags: [Platform Stores]
      security: [{sanctum: []}]
      summary: List Store domains
      description: Requires Platform scope and manage stores. Returns primary first, then hostname order.
      responses:
        '200':
          description: Normalized Store domain records
          content:
            application/json:
              schema: {$ref: '#/components/schemas/StoreDomainsResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Store not found}
    post:
      tags: [Platform Stores]
      security: [{sanctum: []}]
      summary: Add a Store domain
      description: Hostnames are globally unique. The first domain is always primary; making a later domain primary updates stores.primary_domain transactionally.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/CreateStoreDomainRequest'}
      responses:
        '201':
          description: Store domain created
          content:
            application/json:
              schema: {$ref: '#/components/schemas/StoreDomainResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Store not found}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/stores/{store}/domains/{domain}:
    parameters:
      - {name: store, in: path, required: true, schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}, description: Public Store ULID}
      - {name: domain, in: path, required: true, schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}, description: Public Store-domain ULID}
    patch:
      tags: [Platform Stores]
      security: [{sanctum: []}]
      summary: Update a Store domain
      description: Updates hostname/type/routing/SSL/verification state and primary selection. A current primary can only be replaced by making another domain primary.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/UpdateStoreDomainRequest'}
      responses:
        '200':
          description: Store domain updated
          content:
            application/json:
              schema: {$ref: '#/components/schemas/StoreDomainResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Store or domain not found}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/roles:
    get:
      tags: [Store Users]
      security: [{sanctum: []}]
      summary: List roles for the selected Store
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      responses:
        '200':
          description: Store role names
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RoleListResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/store/users:
    get:
      tags: [Store Users]
      security: [{sanctum: []}]
      summary: List users in the selected Store
      parameters:
        - {$ref: '#/components/parameters/StoreId'}
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200':
          description: Paginated Store members, memberships, and Store roles
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ManagedUserListResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [Store Users]
      security: [{sanctum: []}]
      summary: Create a user in the selected Store
      description: Requires manage store members and manage store roles. Only Store roles are accepted.
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/CreateManagedUserRequest'}
      responses:
        '201': {description: Store user and active membership created}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/settings/currencies:
    get:
      tags: [Platform Settings]
      security: [{sanctum: []}]
      summary: List the platform currency catalog
      parameters:
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200':
          description: Paginated platform currency catalog
          content:
            application/json:
              schema:
                type: object
                required: [data, links, meta]
                properties:
                  data:
                    type: array
                    items: {$ref: '#/components/schemas/Currency'}
                  links: {$ref: '#/components/schemas/PaginationLinks'}
                  meta: {$ref: '#/components/schemas/PaginationMeta'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [Platform Settings]
      security: [{sanctum: []}]
      summary: Add a platform currency
      description: Requires a Platform-scoped user with manage platform settings. USD is reserved as the base currency.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/CreateCurrencyRequest'}
      responses:
        '201':
          description: Currency created
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data: {$ref: '#/components/schemas/Currency'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/settings/currencies/{currency}:
    patch:
      tags: [Platform Settings]
      security: [{sanctum: []}]
      summary: Update a platform currency and its USD-relative rate
      description: Requires manage platform settings. Rates mean 1 USD equals X target currency units.
      parameters:
        - name: currency
          in: path
          required: true
          schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/UpdateCurrencyRequest'}
      responses:
        '200':
          description: Currency updated
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data: {$ref: '#/components/schemas/Currency'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Currency not found}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/settings/languages:
    get:
      tags: [Platform Settings]
      security: [{sanctum: []}]
      summary: List the platform language catalog
      parameters:
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200':
          description: Paginated platform language catalog
          content:
            application/json:
              schema:
                type: object
                required: [data, links, meta]
                properties:
                  data:
                    type: array
                    items: {$ref: '#/components/schemas/Language'}
                  links: {$ref: '#/components/schemas/PaginationLinks'}
                  meta: {$ref: '#/components/schemas/PaginationMeta'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [Platform Settings]
      security: [{sanctum: []}]
      summary: Add a supported platform language
      description: Requires a Platform-scoped user with manage platform settings.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/CreateLanguageRequest'}
      responses:
        '201':
          description: Language created
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data: {$ref: '#/components/schemas/Language'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/settings/languages/{language}:
    patch:
      tags: [Platform Settings]
      security: [{sanctum: []}]
      summary: Update a supported platform language
      description: Requires manage platform settings. Locale is immutable after creation.
      parameters:
        - name: language
          in: path
          required: true
          schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/UpdateLanguageRequest'}
      responses:
        '200':
          description: Language updated
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data: {$ref: '#/components/schemas/Language'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Language not found}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/policy-types:
    get:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: Page the Platform policy-type catalog
      parameters: [{$ref: '#/components/parameters/Page'}, {$ref: '#/components/parameters/PerPage'}]
      responses:
        '200': {description: Paginated policy-type catalog}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: Add a custom policy type
      description: Requires Platform scope and manage platform settings. The type is provisioned as a disabled policy for every existing Store.
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/CreatePolicyTypeRequest'}}}
      responses:
        '201': {description: Custom policy type created}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/policy-types/{policyType}:
    parameters:
      - {name: policyType, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    patch:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: Update a policy type
      description: Requires manage platform settings. A system type code is immutable.
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/UpdatePolicyTypeRequest'}}}
      responses:
        '200': {description: Policy type updated}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Policy type not found}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: Delete a custom unreferenced policy type
      responses:
        '204': {description: Policy type deleted}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Policy type not found}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/pages:
    get:
      tags: [Store Pages]
      security: [{sanctum: []}]
      summary: Paginate and filter Pages for the selected Store
      parameters:
        - {$ref: '#/components/parameters/StoreId'}
        - {name: page, in: query, schema: {type: integer, minimum: 1}}
        - {name: per_page, in: query, schema: {type: integer, minimum: 1, maximum: 100, default: 25}}
        - {name: status, in: query, schema: {type: string, enum: [disabled, draft, published]}}
        - {name: page_type, in: query, schema: {type: string, enum: [content, contact, external_link, rss]}}
        - {name: parent_id, in: query, schema: {$ref: '#/components/schemas/PublicUlid'}}
        - {name: root_only, in: query, schema: {type: boolean}}
        - {name: language_id, in: query, schema: {$ref: '#/components/schemas/PublicUlid'}}
        - {name: search, in: query, schema: {type: string, maxLength: 200}}
      responses:
        '200': {description: Paginated Store Page list with translations}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
    post:
      tags: [Store Pages]
      security: [{sanctum: []}]
      summary: Create a draft Page and initial translations
      description: Requires manage policies. All language ULIDs must be active for the selected Store.
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/CreatePageRequest'}}}
      responses:
        '201': {description: Draft Page created}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/pages/{page}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: page, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Store Pages]
      security: [{sanctum: []}]
      summary: Read one Store Page and all translations
      responses:
        '200': {description: Store Page}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Page not found in selected Store}
    patch:
      tags: [Store Pages]
      security: [{sanctum: []}]
      summary: Update language-neutral Page configuration
      description: Requires manage policies. Translation content is managed through the language endpoint.
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/UpdatePageRequest'}}}
      responses:
        '200': {description: Page updated}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Page not found in selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Store Pages]
      security: [{sanctum: []}]
      summary: Disable a Page without deleting hierarchy or translations
      description: Requires manage policies. This non-destructive endpoint clears publication state.
      responses:
        '204': {description: Page disabled}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Page not found in selected Store}
  /api/v1/store/pages/{page}/enable:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: page, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Store Pages]
      security: [{sanctum: []}]
      summary: Enable a disabled Page as a draft
      responses:
        '200': {description: Page enabled}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Page not found in selected Store}
  /api/v1/store/pages/{page}/disable:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: page, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Store Pages]
      security: [{sanctum: []}]
      summary: Disable a Page and clear publication time
      responses:
        '200': {description: Page disabled}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Page not found in selected Store}
  /api/v1/store/pages/{page}/publish:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: page, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Store Pages]
      security: [{sanctum: []}]
      summary: Publish a Page after type and default-language validation
      responses:
        '200': {description: Page published}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/pages/{page}/unpublish:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: page, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Store Pages]
      security: [{sanctum: []}]
      summary: Return a published Page to draft
      responses:
        '200': {description: Page unpublished}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/store/pages/{page}/translations/{language}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: page, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: language, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    put:
      tags: [Store Pages]
      security: [{sanctum: []}]
      summary: Upsert one complete localized Page record
      description: A default-language save may queue every other unlocked active Store language after commit.
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/PageTranslationRequest'}}}
      responses:
        '200': {description: Page translation saved}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Store Pages]
      security: [{sanctum: []}]
      summary: Delete an eligible Page translation
      description: A published Page must retain its default-language translation.
      responses:
        '204': {description: Page translation deleted}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/policy-types:
    get:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: List the ordered policy-type catalog for a Store member
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      responses:
        '200': {description: Complete policy-type catalog}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/store/policies:
    get:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: List policies and translations for the selected Store
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      responses:
        '200': {description: Store policy list}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: Create a disabled Store policy
      description: Requires manage policies. A Store may have one policy per type and slug.
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/CreateStorePolicyRequest'}}}
      responses:
        '201': {description: Disabled policy created}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/policies/{storePolicy}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: storePolicy, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: Read one Store policy and all translations
      responses:
        '200': {description: Store policy}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Policy not found in selected Store}
    patch:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: Update a Store policy title or slug
      description: Requires manage policies.
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/UpdateStorePolicyRequest'}}}
      responses:
        '200': {description: Store policy updated}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Policy not found in selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: Disable a Store policy without deleting content or history
      description: Requires manage policies. This compatibility endpoint clears publication state and preserves the policy row, translations, and versions.
      responses:
        '204': {description: Policy disabled}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Policy not found in selected Store}
  /api/v1/store/policies/{storePolicy}/enable:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: storePolicy, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: Enable a disabled policy as a draft
      responses:
        '200': {description: Policy enabled as draft}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Policy not found in selected Store}
  /api/v1/store/policies/{storePolicy}/disable:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: storePolicy, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: Disable and hide a policy without deleting it
      responses:
        '200': {description: Policy disabled}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Policy not found in selected Store}
  /api/v1/store/policies/{storePolicy}/publish:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: storePolicy, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: Publish an enabled draft policy with at least one non-empty translation
      responses:
        '200': {description: Policy published}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/policies/{storePolicy}/unpublish:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: storePolicy, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: Return a policy to draft
      responses:
        '200': {description: Policy unpublished}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/store/policies/{storePolicy}/translations/{language}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: storePolicy, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: language, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    put:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: Upsert localized policy content and append a content version
      description: Saving the Store's default language commits the source and queues every other unlocked active Store language after commit. Generated content appends versions. Saving a non-default language does not cascade. Provider failures do not roll back the source.
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/StorePolicyTranslationRequest'}}}
      responses:
        '200': {description: Translation saved}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: Delete a translation
      description: A published policy must retain at least one translation.
      responses:
        '204': {description: Translation deleted}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/translation-requests/{translationRequest}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: translationRequest, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Translations]
      security: [{sanctum: []}]
      summary: Read asynchronous translation status for the selected Store
      description: Returns only a request owned by the active Store. It never exposes the provider error or merchant source content.
      responses:
        '200':
          description: Translation request status
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data: {$ref: '#/components/schemas/TranslationRequest'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Translation request not found in selected Store}
  /api/v1/store/policies/{storePolicy}/versions:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: storePolicy, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: List immutable per-language policy versions
      responses:
        '200': {description: Version list}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/store/policies/{storePolicy}/versions/{policyVersion}/restore:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: storePolicy, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: policyVersion, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Store Policies]
      security: [{sanctum: []}]
      summary: Restore version content and append a new version
      responses:
        '201': {description: Restored content version created}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Policy or version not found}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/storefront/policies:
    get:
      tags: [Store Policies]
      summary: List published policies using the best matching locale
      parameters:
        - {$ref: '#/components/parameters/StoreId'}
        - {name: locale, in: query, required: false, schema: {type: string, maxLength: 10}}
      responses:
        '200': {description: Published localized Store policies}
        '404': {description: Store not found}
  /api/v1/storefront/policies/{slug}:
    get:
      tags: [Store Policies]
      summary: Read one published policy by Store-local slug
      parameters:
        - {$ref: '#/components/parameters/StoreId'}
        - {name: slug, in: path, required: true, schema: {type: string, pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'}}
        - {name: locale, in: query, required: false, schema: {type: string, maxLength: 10}}
      responses:
        '200': {description: Published localized Store policy}
        '404': {description: Store or published policy not found}
  /api/v1/store/languages:
    get:
      security: [{sanctum: []}]
      summary: List active catalog languages and Store selection state
      parameters:
        - {$ref: '#/components/parameters/StoreId'}
      responses:
        '200':
          description: Store language settings
          content:
            application/json:
              schema: {$ref: '#/components/schemas/StoreLanguagesResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
    put:
      security: [{sanctum: []}]
      summary: Replace the Store language selection and default
      description: Requires an active Store member with manage store.
      parameters:
        - {$ref: '#/components/parameters/StoreId'}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/UpdateStoreLanguagesRequest'}
      responses:
        '200':
          description: Store language settings updated
          content:
            application/json:
              schema: {$ref: '#/components/schemas/StoreLanguagesResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/settings/fulfillment-types:
    get:
      security: [{sanctum: []}]
      summary: List the global localized Fulfillment Type catalog
      description: Requires an authenticated Platform-scoped account. The catalog is read-only and does not enter Store context.
      responses:
        '200':
          description: Fulfillment Type catalog ordered by sort_order
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FulfillmentTypesResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [Platform Settings]
      security: [{sanctum: []}]
      summary: Create a global Fulfillment Type
      description: Requires manage platform settings. The stable code is immutable after creation.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CreateFulfillmentTypeRequest'}}
      responses:
        '201':
          description: Fulfillment Type created
          content:
            application/json: {schema: {$ref: '#/components/schemas/FulfillmentTypeResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/settings/fulfillment-types/{fulfillmentType}:
    parameters:
      - {name: fulfillmentType, in: path, required: true, schema: {type: string, pattern: '^[a-z][a-z0-9_]*$'}, description: Stable Fulfillment Type code.}
    get:
      tags: [Platform Settings]
      security: [{sanctum: []}]
      summary: Read a global Fulfillment Type by code
      responses:
        '200':
          description: Fulfillment Type with translations
          content:
            application/json: {schema: {$ref: '#/components/schemas/FulfillmentTypeResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Fulfillment Type not found}
    patch:
      tags: [Platform Settings]
      security: [{sanctum: []}]
      summary: Update a global Fulfillment Type
      description: Requires manage platform settings. Supplied translations are upserted by locale; omitted translations remain unchanged.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/UpdateFulfillmentTypeRequest'}}
      responses:
        '200':
          description: Fulfillment Type updated
          content:
            application/json: {schema: {$ref: '#/components/schemas/FulfillmentTypeResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Fulfillment Type not found}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/fulfillment-types:
    get:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: List active Fulfillment Types for a Store editor
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      responses:
        '200':
          description: Active global Fulfillment Types ordered by sort_order
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FulfillmentTypesResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/stores:
    post:
      security: [{sanctum: []}]
      summary: Create an additional Store and Owner membership
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/CreateStoreRequest'}
      responses:
        '201':
          description: Complete draft Store created
          content:
            application/json:
              schema:
                type: object
                required: [data, dashboard_url]
                properties:
                  data: {$ref: '#/components/schemas/Store'}
                  dashboard_url: {type: string, format: uri}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store:
    get:
      security: [{sanctum: []}]
      summary: View the selected Store profile
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      responses:
        '200': {description: Store profile}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Store not found}
  /api/v1/store/profile:
    patch:
      security: [{sanctum: []}]
      summary: Update merchant-owned Store profile fields
      description: Requires manage store. Platform-controlled Billing, lifecycle, verification, entitlement, trial, and raw JSON fields are prohibited.
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/UpdateStoreProfileRequest'}
      responses:
        '200': {description: Updated Store}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/settings:
    get:
      security: [{sanctum: []}]
      summary: View Store locale, opt-in flags, preferences, and read-only capabilities
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      responses:
        '200':
          description: Store settings
          content:
            application/json:
              schema: {$ref: '#/components/schemas/StoreSettingsResponse'}
        '403': {$ref: '#/components/responses/Forbidden'}
    patch:
      security: [{sanctum: []}]
      summary: Merge validated Store locale, opt-in flags, and preferences
      description: Currency and language must exist as active Platform catalog entries. Translation and Platform-search flags persist intent but do not execute background work.
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/UpdateStoreSettingsRequest'}
      responses:
        '200':
          description: Updated Store settings
          content:
            application/json:
              schema: {$ref: '#/components/schemas/StoreSettingsResponse'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/plans:
    get:
      security: [{sanctum: []}]
      summary: List plans with feature/add-on assignments
      parameters:
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200': {description: Paginated plan catalog with data, links, and meta}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      security: [{sanctum: []}]
      summary: Create a plan
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PlanWriteRequest'}
      responses:
        '201': {description: Plan created}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/plans/{plan}:
    parameters:
      - {name: plan, in: path, required: true, schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}}
    get:
      security: [{sanctum: []}]
      summary: View a plan
      responses:
        '200': {description: Plan resource}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Plan not found}
    patch:
      security: [{sanctum: []}]
      summary: Update a plan or its price
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PlanWriteRequest'}
      responses:
        '200': {description: Plan updated}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      security: [{sanctum: []}]
      summary: Remove an unassigned plan
      description: A plan referenced by a Store must be archived instead.
      responses:
        '204': {description: Plan removed}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/features:
    get:
      security: [{sanctum: []}]
      summary: List reusable features
      parameters:
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200': {description: Paginated feature catalog with data, links, and meta}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      security: [{sanctum: []}]
      summary: Create a reusable feature
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/FeatureWriteRequest'}
      responses:
        '201': {description: Feature created}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/features/{feature}:
    parameters:
      - {name: feature, in: path, required: true, schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}}
    patch:
      security: [{sanctum: []}]
      summary: Update a reusable feature
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/FeatureWriteRequest'}
      responses:
        '200': {description: Feature updated}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      security: [{sanctum: []}]
      summary: Remove an unassigned feature
      responses:
        '204': {description: Feature removed}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/plans/{plan}/features/{feature}:
    parameters:
      - {name: plan, in: path, required: true, schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}}
      - {name: feature, in: path, required: true, schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}}
    put:
      security: [{sanctum: []}]
      summary: Add or update a plan feature/add-on
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PlanFeatureWriteRequest'}
      responses:
        '200': {description: Plan feature assignment}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      security: [{sanctum: []}]
      summary: Detach a feature from a plan
      responses:
        '204': {description: Feature detached}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/platform/theme-publishers:
    get:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: List Theme publishers
      description: Requires Platform scope and manage marketplace.
      parameters:
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200': {description: Paginated publisher catalog}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Create a Theme publisher
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ThemePublisherWriteRequest'}
      responses:
        '201': {description: Publisher created}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/theme-publishers/{themePublisher}:
    parameters:
      - {name: themePublisher, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    patch:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Update a Theme publisher
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ThemePublisherWriteRequest'}
      responses:
        '200': {description: Publisher updated}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/theme-categories:
    get:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: List Theme taxonomy and facets
      parameters:
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200': {description: Paginated Theme categories}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Create a Theme category
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ThemeCategoryWriteRequest'}
      responses:
        '201': {description: Category created}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/theme-categories/{themeCategory}:
    parameters:
      - {name: themeCategory, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    patch:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Update a Theme category
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ThemeCategoryWriteRequest'}
      responses:
        '200': {description: Category updated}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/themes:
    get:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: List marketplace Themes
      description: Requires Platform scope and manage marketplace.
      parameters:
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
        - {name: search, in: query, schema: {type: string, maxLength: 160}}
        - {name: source_type, in: query, schema: {type: string, enum: [platform, third_party, custom]}}
        - {name: status, in: query, schema: {type: string, enum: [draft, pending_review, approved, published, suspended, rejected, retired]}}
        - {name: visibility, in: query, schema: {type: string, enum: [public, private, unlisted]}}
      responses:
        '200': {description: Paginated Theme listings}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Create a Theme listing
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ThemeWriteRequest'}
      responses:
        '201': {description: Theme created}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/themes/{theme}:
    parameters:
      - {name: theme, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: View a Theme with versions and submissions
      responses:
        '200':
          description: Theme detail
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ThemeResponse'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Theme not found}
    patch:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Update a Theme listing
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ThemeWriteRequest'}
      responses:
        '200': {description: Theme updated}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/themes/{theme}/versions:
    parameters:
      - {name: theme, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Register an immutable Theme version
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ThemeVersionWriteRequest'}
      responses:
        '201': {description: Version registered}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/theme-versions/{themeVersion}/submit:
    parameters:
      - {name: themeVersion, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Submit a Theme version for review
      responses:
        '201': {description: Numbered submission created}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/theme-submissions/{themeSubmission}/review:
    parameters:
      - {name: themeSubmission, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    patch:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Decide a Theme review submission
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ThemeReviewRequest'}
      responses:
        '200': {description: Review decision recorded}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/theme-versions/{themeVersion}/publish:
    parameters:
      - {name: themeVersion, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Publish an approved immutable Theme version
      responses:
        '200': {description: Theme current-version pointer updated}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/themes/{theme}/licenses:
    parameters:
      - {name: theme, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Issue a Theme license to a Store
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ThemeLicenseIssueRequest'}
      responses:
        '201': {description: License issued}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/platform/theme-licenses/{themeLicense}:
    parameters:
      - {name: themeLicense, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    patch:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Update a Theme license lifecycle
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: {type: string, enum: [active, revoked, transferred, refunded, expired]}
      responses:
        '200': {description: License updated}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/theme-marketplace:
    get:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: List Themes installable by the selected Store
      description: Requires active Store membership and manage themes.
      parameters:
        - {$ref: '#/components/parameters/StoreId'}
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
        - {name: search, in: query, schema: {type: string, maxLength: 160}}
      responses:
        '200': {description: Paginated marketplace Themes}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/store/themes:
    get:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: List installed Theme copies
      parameters:
        - {$ref: '#/components/parameters/StoreId'}
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200': {description: Paginated Store Theme installations}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Install a Theme as a draft Store copy
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/StoreThemeInstallRequest'}
      responses:
        '201': {description: Theme installed}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/themes/{storeTheme}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: storeTheme, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    patch:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Update a Store Theme copy with optimistic revision control
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/StoreThemeUpdateRequest'}
      responses:
        '200': {description: Store Theme updated with incremented revision}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Soft-delete a non-published Store Theme copy
      responses:
        '204': {description: Store Theme deleted}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/themes/{storeTheme}/duplicate:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: storeTheme, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Duplicate an installed Theme into a draft
      responses:
        '201': {description: Draft duplicate created}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/store/themes/{storeTheme}/publish:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: storeTheme, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Themes]
      security: [{sanctum: []}]
      summary: Publish this copy and archive the former published copy
      responses:
        '200': {description: Store Theme published}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/media:
    get:
      tags: [Store Media]
      security: [{sanctum: []}]
      summary: Page non-deleted media for the selected Store
      parameters:
        - {$ref: '#/components/parameters/StoreId'}
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
        - {name: status, in: query, schema: {type: string, enum: [pending, processing, ready, failed]}}
        - {name: mime_type, in: query, schema: {type: string, enum: [image/jpeg, image/png, image/gif, image/webp, image/avif]}}
        - {name: search, in: query, schema: {type: string, maxLength: 255}}
      responses:
        '200': {description: Paginated Store media, content locators, and derivatives, content: {application/json: {schema: {$ref: '#/components/schemas/MediaPageResponse'}}}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
  /api/v1/store/media/uploads:
    post:
      tags: [Store Media]
      security: [{sanctum: []}]
      summary: Upload a pending reusable media object
      description: Requires manage products. The server verifies actual MIME plus extension and writes through an allow-listed Laravel Storage disk.
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      requestBody:
        required: true
        content:
          multipart/form-data: {schema: {$ref: '#/components/schemas/CreateMediaUploadRequest'}}
      responses:
        '201': {description: Pending master media created, content: {application/json: {schema: {$ref: '#/components/schemas/MediaResponse'}}}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/media/{media}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: media, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Store Media]
      security: [{sanctum: []}]
      summary: Read one non-deleted media asset from the selected Store
      responses:
        '200': {description: Master media and derivatives, content: {application/json: {schema: {$ref: '#/components/schemas/MediaResponse'}}}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Media not found in the selected Store}
    delete:
      tags: [Store Media]
      security: [{sanctum: []}]
      summary: Logically delete media and detach active Product/Variant relationships
      description: Requires manage products. The row, usages, original, and derivatives are retained for recovery.
      responses:
        '204': {description: Media marked deleted and detached}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Media not found in the selected Store}
  /api/v1/store/media/{media}/complete:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: media, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Store Media]
      security: [{sanctum: []}]
      summary: Complete an upload and queue processing
      description: Requires manage products. Idempotently queues metadata extraction, optimization, derivative generation, and finalization.
      responses:
        '202': {description: Media processing accepted, content: {application/json: {schema: {$ref: '#/components/schemas/MediaResponse'}}}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Media not found in the selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/media/{media}/content:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: media, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: variant, in: query, schema: {type: string, enum: [original, thumbnail, small, medium, large]}}
    get:
      tags: [Store Media]
      security: [{sanctum: []}]
      summary: Stream an original or generated media variant
      responses:
        '200': {description: Authenticated media bytes}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Media or variant not found in the selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/products/{product}/media:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Store Media]
      security: [{sanctum: []}]
      summary: Attach ready media to a Product
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/AttachProductMediaRequest'}}}}
      responses:
        '201': {description: Product media attached}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product or ready media not found in the selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/products/{product}/media/{media}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: media, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    delete:
      tags: [Store Media]
      security: [{sanctum: []}]
      summary: Detach media from a Product
      responses:
        '204': {description: Product media detached}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product, media, or attachment not found in the selected Store}
  /api/v1/store/products/{product}/media/{media}/primary:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: media, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    put:
      tags: [Store Media]
      security: [{sanctum: []}]
      summary: Make attached media the Product primary
      responses:
        '200': {description: Product primary media changed atomically}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product, media, or attachment not found in the selected Store}
  /api/v1/store/product-variants/{variant}/media:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: variant, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Store Media]
      security: [{sanctum: []}]
      summary: Attach ready media to a Product Variant
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/AttachVariantMediaRequest'}}}}
      responses:
        '201': {description: Product Variant media attached}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Variant or ready media not found in the selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/product-variants/{variant}/media/{media}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: variant, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: media, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    delete:
      tags: [Store Media]
      security: [{sanctum: []}]
      summary: Detach media from a Product Variant
      responses:
        '204': {description: Product Variant media detached}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Variant, media, or attachment not found in the selected Store}
  /api/v1/store/product-detail:
    get:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Bootstrap the Store Product editor in one request
      description: Returns bounded Brand, Category, Product Type, taxonomy, fulfillment, Custom Field, shared-option, Modifier, language, currency, and Store-default reference data.
      parameters:
        - {$ref: '#/components/parameters/StoreId'}
        - {name: reference_limit, in: query, schema: {type: integer, minimum: 1, maximum: 500, default: 250}}
        - {name: sections, in: query, description: Optional comma-separated registered section keys; omission returns every section. Product is accepted as an explicit core marker., schema: {type: string, pattern: '^[a-z][a-z0-9_]*(,[a-z][a-z0-9_]*)*$', example: 'product,images,options'}}
      responses:
        '200':
          description: Product editor reference data
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductDetailResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
    post:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Intelligently create a Product and supplied sections
      description: Requires manage products. Catalog-owned Product core and supplied sections are delegated to their existing services and committed in one outer transaction. Binary media must already be uploaded.
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CreateProductDetailRequest'}}
      responses:
        '201':
          description: Product aggregate created
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductDetailResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/product-detail/{product}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Read a complete Product editor aggregate
      parameters:
        - {name: section_limit, in: query, schema: {type: integer, minimum: 1, maximum: 250, default: 100}}
        - {name: reference_limit, in: query, schema: {type: integer, minimum: 1, maximum: 500, default: 250}}
        - {name: with_reference_data, in: query, schema: {type: boolean, default: true}}
        - {name: sections, in: query, description: Optional comma-separated registered section keys. Only named section queries/providers run; Product core and revision remain present., schema: {type: string, pattern: '^[a-z][a-z0-9_]*(,[a-z][a-z0-9_]*)*$', example: 'product,images,discounts'}}
      responses:
        '200':
          description: Product core, Product-owned sections, selector references, section counts, and revision
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductDetailResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product not found in selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
    patch:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Intelligently update only supplied Product fields and sections
      description: Requires manage products. Omitted sections are unchanged. A stale expected_updated_at returns 409.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/UpdateProductDetailRequest'}}
      responses:
        '200':
          description: Refreshed Product aggregate with saved_sections and request-local reference mappings
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductDetailResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product not found in selected Store}
        '409': {description: Product revision is stale; reload before reapplying pending sections}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/collections:
    get:
      tags: [Catalog Collections]
      security: [{sanctum: []}]
      summary: Page Collections for the selected Store
      parameters:
        - {$ref: '#/components/parameters/StoreId'}
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
        - {name: search, in: query, schema: {type: string, maxLength: 200}}
        - {name: locale, in: query, schema: {type: string, maxLength: 35}}
        - {name: parent_id, in: query, schema: {$ref: '#/components/schemas/PublicUlid'}}
        - {name: root_only, in: query, schema: {type: boolean}}
        - {name: is_active, in: query, schema: {type: boolean}}
        - {name: collection_type, in: query, schema: {type: string, enum: [manual, rule_based, ai_generated]}}
        - {name: sort_by, in: query, schema: {type: string, enum: [sort_order, created_at, updated_at]}}
        - {name: sort_direction, in: query, schema: {type: string, enum: [asc, desc]}}
      responses:
        '200': {description: Paginated Store Collections with translations and aggregate counts}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
    post:
      tags: [Catalog Collections]
      security: [{sanctum: []}]
      summary: Create a Store Collection
      description: Requires manage products. Optional rules and manual Product memberships are committed with the Collection.
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CreateCollectionRequest'}}
      responses:
        '201': {description: Collection created}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/collections/{collection}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: collection, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Collections]
      security: [{sanctum: []}]
      summary: Read one Collection with translations, rules, and aggregate counts
      responses:
        '200': {description: Store Collection}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Collection not found in the selected Store}
    patch:
      tags: [Catalog Collections]
      security: [{sanctum: []}]
      summary: Update one Collection
      description: Requires manage products. Omitted translations, rules, and Products are unchanged. Changing collection_type to manual clears rules and unpinned automated memberships.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CollectionWriteRequest'}}
      responses:
        '200': {description: Collection updated}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Collection not found in the selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Catalog Collections]
      security: [{sanctum: []}]
      summary: Delete one Collection and its dependent rows
      description: Requires manage products. Children remain and their parent_id becomes null.
      responses:
        '204': {description: Collection deleted}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Collection not found in the selected Store}
  /api/v1/store/collections/{collection}/rules:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: collection, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    put:
      tags: [Catalog Collections]
      security: [{sanctum: []}]
      summary: Replace the complete rule set for an automated Collection
      description: Requires manage products. Manual Collections reject rules. This does not refresh membership until the refresh operation is called.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CollectionRuleReplacementRequest'}}
      responses:
        '200': {description: Collection with replaced rules}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Collection not found in the selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/collections/{collection}/products:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: collection, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Collections]
      security: [{sanctum: []}]
      summary: Page the Products assigned to one Collection
      parameters:
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200': {description: Paginated Product memberships including source, pin, and order metadata}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Collection not found in the selected Store}
    put:
      tags: [Catalog Collections]
      security: [{sanctum: []}]
      summary: Replace the complete set of manual Product memberships
      description: Requires manage products. Automated rows are preserved; matching submitted rows become manual.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CollectionProductReplacementRequest'}}
      responses:
        '200': {description: Collection with refreshed membership count}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Collection or submitted Product not found in the selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/collections/{collection}/refresh:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: collection, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Catalog Collections]
      security: [{sanctum: []}]
      summary: Evaluate saved rules and refresh automated Product memberships
      description: Requires manage products. The transaction deletes only unpinned rule/AI rows, preserves manual and pinned rows, and inserts current matches.
      responses:
        '200': {description: Refreshed Collection plus meta.matched_count}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Collection not found in the selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/collections/{collection}/ai-jobs:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: collection, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Collections]
      security: [{sanctum: []}]
      summary: Page append-only AI Collection job history
      description: Read-only history in this release; no REST operation starts an AI rule-generation job.
      parameters:
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200': {description: Paginated AI job audit records}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Collection not found in the selected Store}
  /api/v1/store/products:
    get:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Page Products for the selected Store
      parameters:
        - {$ref: '#/components/parameters/StoreId'}
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
        - {name: search, in: query, schema: {type: string, maxLength: 200}}
        - {name: locale, in: query, schema: {type: string, maxLength: 35}}
        - {name: sku, in: query, schema: {type: string, maxLength: 250}}
        - {name: status, in: query, schema: {type: string, enum: [draft, active, archived]}}
        - {name: fulfillment_type, in: query, schema: {type: string, enum: [physical, digital, software, service]}}
        - {name: condition, in: query, schema: {type: string, enum: [New, Used, Refurbished]}}
        - {name: is_featured, in: query, schema: {type: boolean}}
        - {name: brand_id, in: query, schema: {$ref: '#/components/schemas/PublicUlid'}}
        - {name: category_id, in: query, schema: {$ref: '#/components/schemas/PublicUlid'}}
        - {name: sort_by, in: query, schema: {type: string, enum: [created_at, updated_at, status, published_at, price, sort_order], default: created_at}}
        - {name: sort_direction, in: query, schema: {type: string, enum: [asc, desc], default: desc}}
      responses:
        '200':
          description: Paginated Store Products with commerce fields and translations
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductPageResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Create a Product
      description: Requires manage products and at least one active Store-locale translation. Translation work is recorded transactionally and dispatched after commit.
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CreateProductRequest'}}
      responses:
        '201':
          description: Product created
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/products/{product}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Read a Product from the selected Store
      responses:
        '200':
          description: Product with commerce fields, classifications, categories, and translations
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product not found in selected Store}
    patch:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Update a Product
      description: Requires manage products. Omitted fields remain unchanged; supplied category IDs replace assignments atomically.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/UpdateProductRequest'}}
      responses:
        '200':
          description: Product updated
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product not found in selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Delete a Product
      description: Requires manage products. Product-owned translations and relationships cascade; shared Catalog entities remain.
      responses:
        '204': {description: Product deleted}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product not found in selected Store}
  /api/v1/store/products/{product}/images:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Page image metadata for a Product
      description: Returns image metadata ordered by position and identity. Requires active membership in the selected Store.
      parameters:
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200':
          description: Paginated Product image metadata
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductImagePageResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product not found in selected Store}
    post:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Add image metadata to a Product
      description: Requires manage products. This writes a root-relative or HTTP(S) locator and metadata; it does not upload a binary object.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CreateProductImageRequest'}}
      responses:
        '201':
          description: Product image metadata created
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductImageResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product or supplied variant not found in the nested Store/Product boundary}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/products/{product}/images/{image}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: image, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Read nested Product image metadata
      responses:
        '200':
          description: Product image metadata
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductImageResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product or image not found in the selected Store and nested Product boundary}
    patch:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Partially update nested Product image metadata
      description: Requires manage products. Omitted fields remain unchanged; variant_id null removes the variant association.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/UpdateProductImageRequest'}}
      responses:
        '200':
          description: Product image metadata updated
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductImageResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product, image, or supplied variant not found in the nested Store/Product boundary}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Delete nested Product image metadata
      description: Requires manage products. This does not delete any media object referenced by url.
      responses:
        '204': {description: Product image metadata deleted}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product or image not found in the selected Store and nested Product boundary}
  /api/v1/store/custom-fields:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
    get:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: Page custom-field definitions
      description: Requires active Store membership. A product_type filter includes global definitions plus definitions for that Product Type code.
      parameters:
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
        - {name: search, in: query, schema: {type: string, maxLength: 200}}
        - {name: product_type, in: query, schema: {type: string, maxLength: 255}}
        - {name: field_key, in: query, schema: {type: string, maxLength: 100}}
        - {name: field_type, in: query, schema: {$ref: '#/components/schemas/CustomFieldType'}}
        - {name: is_required, in: query, schema: {type: boolean}}
        - {name: is_filterable, in: query, schema: {type: boolean}}
        - {name: sort_by, in: query, schema: {type: string, enum: [position, field_key, created_at, updated_at]}}
        - {name: sort_direction, in: query, schema: {type: string, enum: [asc, desc]}}
      responses:
        '200':
          description: Paginated custom-field definitions
          content:
            application/json: {schema: {$ref: '#/components/schemas/CustomFieldDefinitionPageResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
    post:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: Create a custom-field definition
      description: Requires manage products. Initial options are accepted only for select or multi_select fields.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CreateCustomFieldDefinitionRequest'}}
      responses:
        '201':
          description: Custom-field definition created
          content:
            application/json: {schema: {$ref: '#/components/schemas/CustomFieldDefinitionResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/custom-fields/{definition}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: definition, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: Read a custom-field definition
      responses:
        '200':
          description: Definition with translations, ordered options, and usage count
          content:
            application/json: {schema: {$ref: '#/components/schemas/CustomFieldDefinitionResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Definition not found in selected Store}
    patch:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: Partially update a custom-field definition
      description: Requires manage products. The field type cannot change after values exist.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/UpdateCustomFieldDefinitionRequest'}}
      responses:
        '200':
          description: Custom-field definition updated
          content:
            application/json: {schema: {$ref: '#/components/schemas/CustomFieldDefinitionResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Definition not found in selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: Delete a custom-field definition and its owned values
      description: Requires manage products. Database cascade rules remove its translations, options, and Product/Variant values.
      responses:
        '204': {description: Custom-field definition deleted}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Definition not found in selected Store}
  /api/v1/store/custom-fields/{definition}/options:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: definition, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: List ordered options for a select custom field
      responses:
        '200':
          description: Custom-field options
          content:
            application/json: {schema: {$ref: '#/components/schemas/CustomFieldOptionListResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Definition not found in selected Store}
    post:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: Add an option to a select or multi-select field
      description: Requires manage products.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CreateCustomFieldOptionRequest'}}
      responses:
        '201':
          description: Custom-field option created
          content:
            application/json: {schema: {$ref: '#/components/schemas/CustomFieldOptionResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Definition not found in selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/custom-fields/{definition}/options/{option}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: definition, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: option, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: Read a custom-field option
      responses:
        '200':
          description: Custom-field option
          content:
            application/json: {schema: {$ref: '#/components/schemas/CustomFieldOptionResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Definition or option not found in nested Store boundary}
    patch:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: Update option order or submitted translations
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/UpdateCustomFieldOptionRequest'}}
      responses:
        '200':
          description: Custom-field option updated
          content:
            application/json: {schema: {$ref: '#/components/schemas/CustomFieldOptionResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Definition or option not found in nested Store boundary}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: Delete an unused custom-field option
      description: Requires manage products. Selected options must first be removed from all Product and Variant values.
      responses:
        '204': {description: Custom-field option deleted}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Definition or option not found in nested Store boundary}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/products/{product}/custom-field-values:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: List Product-level custom-field values
      responses:
        '200':
          description: Product-level custom-field values only; Variant values are excluded
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductCustomFieldValueListResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product not found in selected Store}
  /api/v1/store/products/{product}/custom-field-values/{definition}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: definition, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: Read one Product-level custom-field value
      responses:
        '200':
          description: Product custom-field value
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductCustomFieldValueResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product, definition, or value not found}
    put:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: Set or replace one Product-level typed value
      description: Requires manage products. The body must contain exactly the property required by the definition field_type.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/ProductCustomFieldValueRequest'}}
      responses:
        '200':
          description: Product custom-field value set
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductCustomFieldValueResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product, definition, or selected option not found}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: Unset one Product-level custom-field value
      responses:
        '204': {description: Product custom-field value deleted}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product, definition, or value not found}
  /api/v1/store/products/{product}/variants/{variant}/custom-field-values:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: variant, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: List Variant-level custom-field values
      responses:
        '200':
          description: Variant custom-field values
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductCustomFieldValueListResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product or Variant not found in nested Store boundary}
  /api/v1/store/products/{product}/variants/{variant}/custom-field-values/{definition}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: variant, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: definition, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: Read one Variant-level custom-field value
      responses:
        '200':
          description: Variant custom-field value
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductCustomFieldValueResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product, Variant, definition, or value not found}
    put:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: Set or replace one Variant-level typed value
      description: Requires manage products. The Variant must belong to the Product, and the body shape must match field_type.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/ProductCustomFieldValueRequest'}}
      responses:
        '200':
          description: Variant custom-field value set
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductCustomFieldValueResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product, Variant, definition, or selected option not found}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Catalog Custom Fields]
      security: [{sanctum: []}]
      summary: Unset one Variant-level custom-field value
      responses:
        '204': {description: Variant custom-field value deleted}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product, Variant, definition, or value not found}
  /api/v1/store/options:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
    get:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: List reusable shared Product option definitions
      parameters:
        - {name: page, in: query, schema: {type: integer, minimum: 1}}
        - {name: per_page, in: query, schema: {type: integer, minimum: 1, maximum: 100}}
        - {name: search, in: query, schema: {type: string, maxLength: 200}}
        - {name: type, in: query, schema: {$ref: '#/components/schemas/SharedProductOptionType'}}
      responses:
        '200':
          description: Paginated shared Product options with translations, Values, and usage counts
          content:
            application/json: {schema: {$ref: '#/components/schemas/SharedProductOptionPageResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
    post:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Create a reusable shared Product option definition
      description: Requires manage products. Internal names are case-insensitively unique per Store and at most one Value may be default.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CreateSharedProductOptionRequest'}}
      responses:
        '201':
          description: Shared Product option created
          content:
            application/json: {schema: {$ref: '#/components/schemas/SharedProductOptionResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/options/{option}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: option, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Read a shared Product option definition
      responses:
        '200':
          description: Shared Product option with all translations and ordered Values
          content:
            application/json: {schema: {$ref: '#/components/schemas/SharedProductOptionResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Shared Product option not found in selected Store}
    patch:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Update a shared Product option definition and submitted Value collection
      description: Requires manage products. When values is present, Values omitted from that collection are deleted.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/UpdateSharedProductOptionRequest'}}
      responses:
        '200':
          description: Shared Product option updated
          content:
            application/json: {schema: {$ref: '#/components/schemas/SharedProductOptionResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Shared Product option or submitted Value not found in selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Delete an unassigned shared Product option
      description: Requires manage products. Every Product assignment must be removed first.
      responses:
        '204': {description: Shared Product option deleted}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Shared Product option not found in selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/products/{product}/shared-options:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: List shared option assignments for a Product
      responses:
        '200':
          description: Ordered Product shared-option assignments
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductSharedOptionAssignmentListResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product not found in selected Store}
    post:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Assign a shared option definition to a Product
      description: Requires manage products. Repeating the same Product/Option pair updates its position.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CreateProductSharedOptionAssignmentRequest'}}
      responses:
        '201':
          description: Shared option assigned to Product
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductSharedOptionAssignmentResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product or shared option not found in selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/products/{product}/shared-options/{assignment}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: assignment, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    delete:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Remove a shared option assignment from a Product
      description: Requires manage products. The shared definition and its translations remain available.
      responses:
        '204': {description: Product shared-option assignment deleted}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product or assignment not found in the nested Store/Product boundary}
  /api/v1/store/products/{product}/options:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: List ordered multilingual option dimensions for a Product
      responses:
        '200':
          description: Product options with all translated values
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductOptionListResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product not found in selected Store}
    post:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Create a translated Product option and optional initial values
      description: Requires manage products. Adding an option is rejected while variants exist.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CreateProductOptionRequest'}}
      responses:
        '201':
          description: Product option created
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductOptionResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product not found in selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/products/{product}/options/{option}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: option, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Read a nested Product option with translated values
      responses:
        '200':
          description: Product option
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductOptionResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product or option not found in the nested Store/Product boundary}
    patch:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Update option order or submitted locale rows
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/UpdateProductOptionRequest'}}
      responses:
        '200':
          description: Product option updated
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductOptionResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product or option not found in the nested Store/Product boundary}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Delete an option when the Product has no variants
      responses:
        '204': {description: Product option deleted}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product or option not found in the nested Store/Product boundary}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/products/{product}/options/{option}/values:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: option, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: List translated values for a Product option
      responses:
        '200':
          description: Ordered Product option values
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductOptionValueListResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product or option not found}
    post:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Add a translated value to a Product option
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CreateProductOptionValueRequest'}}
      responses:
        '201':
          description: Product option value created
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductOptionValueResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product or option not found}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/products/{product}/options/{option}/values/{value}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: option, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: value, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Read one translated Product option value
      responses:
        '200':
          description: Product option value
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductOptionValueResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product, option, or value not found in the nested boundary}
    patch:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Update value order or submitted locale rows
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/UpdateProductOptionValueRequest'}}
      responses:
        '200':
          description: Product option value updated
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductOptionValueResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product, option, or value not found in the nested boundary}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Delete an option value that no variant selects
      responses:
        '204': {description: Product option value deleted}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product, option, or value not found in the nested boundary}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/products/{product}/variants:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Page multilingual sellable variants for a Product
      parameters:
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200':
          description: Paginated Product variants
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductVariantPageResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product not found in selected Store}
    post:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Create a complete Product variant combination
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CreateProductVariantRequest'}}
      responses:
        '201':
          description: Product variant created
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductVariantResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product or preferred image not found in the nested boundary}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/products/{product}/variants/{variant}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: product, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: variant, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Read one multilingual Product variant
      responses:
        '200':
          description: Product variant
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductVariantResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product or variant not found in the nested boundary}
    patch:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Update commerce fields, translations, or the complete selection
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/UpdateProductVariantRequest'}}
      responses:
        '200':
          description: Product variant updated
          content:
            application/json: {schema: {$ref: '#/components/schemas/ProductVariantResponse'}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product, variant, or preferred image not found in the nested boundary}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Catalog Products]
      security: [{sanctum: []}]
      summary: Delete a Product variant and synchronize has_variants
      responses:
        '204': {description: Product variant deleted}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Product or variant not found in the nested boundary}
  /api/v1/store/brands:
    get:
      tags: [Catalog Brands]
      security: [{sanctum: []}]
      summary: Page Brands for the selected Store
      parameters:
        - {$ref: '#/components/parameters/StoreId'}
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200': {description: Paginated Store Brands with translations}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [Catalog Brands]
      security: [{sanctum: []}]
      summary: Create a Brand with managed imagery and localized identity
      description: Requires manage products and at least one active Store-locale translation. The source is committed first; missing active locales are queued after commit from the submitted default-locale or first translation. Locked records are preserved and provider failure does not roll back the Brand.
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CreateBrandRequest'}}
          multipart/form-data: {schema: {$ref: '#/components/schemas/CreateBrandRequest'}}
      responses:
        '201': {description: Brand created}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/brands/{brand}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: brand, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Catalog Brands]
      security: [{sanctum: []}]
      summary: Read a Brand from the selected Store
      responses:
        '200': {description: Brand with translations}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Brand not found in selected Store}
    patch:
      tags: [Catalog Brands]
      security: [{sanctum: []}]
      summary: Update Brand identity, managed imagery, or Store-language translations
      description: Requires manage products. The edit commits first, then a translation-bearing update queues every unlocked active Store locale through the server-side OpenAI integration while preserving locked rows. Provider failures are retried without rolling back the write.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/UpdateBrandRequest'}}
          multipart/form-data: {schema: {$ref: '#/components/schemas/UpdateBrandRequest'}}
      responses:
        '200': {description: Brand updated}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Brand not found in selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Catalog Brands]
      security: [{sanctum: []}]
      summary: Delete a Brand
      description: Requires manage products. Translations and managed media are removed; product brand references become null.
      responses:
        '204': {description: Brand deleted}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Brand not found in selected Store}
  /api/v1/store/brands/{brand}/media/{collection}:
    get:
      tags: [Catalog Brands]
      summary: Stream Brand media from private storage
      description: Uses the short-lived relative `expires` and `signature` query values emitted in authenticated Brand responses. Only the `image` and `banner` collections are addressable.
      parameters:
        - {name: brand, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
        - {name: collection, in: path, required: true, schema: {type: string, enum: [image, banner]}}
        - {name: expires, in: query, required: true, schema: {type: integer}}
        - {name: signature, in: query, required: true, schema: {type: string}}
      responses:
        '200':
          description: Brand media bytes
          headers:
            Cache-Control: {schema: {type: string}, description: Private short-lived browser cache directive.}
          content:
            image/avif: {schema: {type: string, contentEncoding: binary}}
            image/jpeg: {schema: {type: string, contentEncoding: binary}}
            image/png: {schema: {type: string, contentEncoding: binary}}
            image/webp: {schema: {type: string, contentEncoding: binary}}
        '403': {description: Signed URL is missing, modified, or expired}
        '404': {description: Brand or requested media does not exist}
  /api/v1/store/customers:
    get:
      tags: [Customers]
      security: [{sanctum: []}]
      summary: Page and search customers in the selected Store
      parameters:
        - {$ref: '#/components/parameters/StoreId'}
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
        - {name: search, in: query, schema: {type: string, maxLength: 255}}
        - {name: status, in: query, schema: {$ref: '#/components/schemas/CustomerStatus'}}
        - {name: customer_group_id, in: query, schema: {$ref: '#/components/schemas/PublicUlid'}}
      responses:
        '200': {description: Paginated selected-Store customers with ledger balances}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [Customers]
      security: [{sanctum: []}]
      summary: Create a customer profile
      description: Requires manage customers. Password, hash, token, and legacy identifier fields are prohibited. An omitted group uses the Store default when present.
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CreateCustomerRequest'}}
      responses:
        '201':
          description: Customer created
          content: {application/json: {schema: {$ref: '#/components/schemas/CustomerEnvelope'}}}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/customers/{customer}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: customer, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Customers]
      security: [{sanctum: []}]
      summary: Read one selected-Store customer
      responses:
        '200':
          description: Customer profile
          content: {application/json: {schema: {$ref: '#/components/schemas/CustomerEnvelope'}}}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Customer not found in selected Store}
    patch:
      tags: [Customers]
      security: [{sanctum: []}]
      summary: Update a customer profile
      description: Requires manage customers. Supplying null customer_group_id explicitly removes the assignment.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CustomerWriteRequest'}}
      responses:
        '200':
          description: Customer updated
          content: {application/json: {schema: {$ref: '#/components/schemas/CustomerEnvelope'}}}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Customer not found in selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Customers]
      security: [{sanctum: []}]
      summary: Disable and soft-delete a customer
      description: Requires manage customers. Credit history is preserved; no physical-delete API is exposed.
      responses:
        '204': {description: Customer disabled and soft-deleted}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Customer not found in selected Store}
  /api/v1/store/customers/{customer}/credits:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: customer, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Customers]
      security: [{sanctum: []}]
      summary: Page append-only customer credit entries
      parameters:
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
      responses:
        '200': {description: Paginated signed credit ledger}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Customer not found in selected Store}
    post:
      tags: [Customers]
      security: [{sanctum: []}]
      summary: Append a customer credit entry
      description: Requires manage customers. There is no update/delete route; corrections use compensating adjustments.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CreateCustomerCreditRequest'}}
      responses:
        '201':
          description: Credit entry appended
          content: {application/json: {schema: {$ref: '#/components/schemas/CustomerCreditEnvelope'}}}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Customer not found in selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/customer-groups:
    get:
      tags: [Customers]
      security: [{sanctum: []}]
      summary: Page and search customer groups
      parameters:
        - {$ref: '#/components/parameters/StoreId'}
        - {$ref: '#/components/parameters/Page'}
        - {$ref: '#/components/parameters/PerPage'}
        - {name: search, in: query, schema: {type: string, maxLength: 255}}
      responses:
        '200': {description: Paginated groups with translations, category IDs, and target discounts}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [Customers]
      security: [{sanctum: []}]
      summary: Create a customer group and default-language display name
      description: Requires manage customers. Only display-name translations are multilingual; code and discount/access logic are stable fields.
      parameters: [{$ref: '#/components/parameters/StoreId'}]
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CreateCustomerGroupRequest'}}
      responses:
        '201':
          description: Group created and optional after-commit translation requested
          content: {application/json: {schema: {$ref: '#/components/schemas/CustomerGroupEnvelope'}}}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/customer-groups/{customerGroup}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: customerGroup, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    get:
      tags: [Customers]
      security: [{sanctum: []}]
      summary: Read one customer group
      responses:
        '200':
          description: Group detail
          content: {application/json: {schema: {$ref: '#/components/schemas/CustomerGroupEnvelope'}}}
        '404': {description: Group not found in selected Store}
    patch:
      tags: [Customers]
      security: [{sanctum: []}]
      summary: Update customer-group logic fields
      description: Requires manage customers. Translations and category assignments use their dedicated endpoints.
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/UpdateCustomerGroupRequest'}}
      responses:
        '200':
          description: Group updated
          content: {application/json: {schema: {$ref: '#/components/schemas/CustomerGroupEnvelope'}}}
        '404': {description: Group not found in selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Customers]
      security: [{sanctum: []}]
      summary: Delete an unused non-default customer group
      responses:
        '204': {description: Group deleted}
        '404': {description: Group not found in selected Store}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/customer-groups/{customerGroup}/translations/{language}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: customerGroup, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: language, in: path, required: true, description: Active Store Language public ULID, schema: {$ref: '#/components/schemas/PublicUlid'}}
    put:
      tags: [Customers, Translations]
      security: [{sanctum: []}]
      summary: Upsert one customer-group display-name translation
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CustomerGroupTranslationWriteRequest'}}
      responses:
        '200': {description: Translation saved; default-language writes may return translation_request}
        '404': {description: Group or active Store language not found}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Customers, Translations]
      security: [{sanctum: []}]
      summary: Delete a non-default customer-group translation
      responses:
        '204': {description: Translation deleted}
        '404': {description: Group or translation not found}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/customer-groups/{customerGroup}/categories:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: customerGroup, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    put:
      tags: [Customers]
      security: [{sanctum: []}]
      summary: Replace the specific Category access allow-list
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/ReplaceCustomerGroupCategoriesRequest'}}
      responses:
        '200':
          description: Complete Category assignment replaced
          content: {application/json: {schema: {$ref: '#/components/schemas/CustomerGroupEnvelope'}}}
        '404': {description: Group or same-Store Category not found}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/customer-groups/{customerGroup}/discounts:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: customerGroup, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    post:
      tags: [Customers]
      security: [{sanctum: []}]
      summary: Create a Category or Product group discount
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CustomerGroupDiscountWriteRequest'}}
      responses:
        '201':
          description: Target discount created
          content: {application/json: {schema: {$ref: '#/components/schemas/CustomerGroupDiscountEnvelope'}}}
        '404': {description: Group or same-Store Catalog target not found}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/store/customer-groups/{customerGroup}/discounts/{discount}:
    parameters:
      - {$ref: '#/components/parameters/StoreId'}
      - {name: customerGroup, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
      - {name: discount, in: path, required: true, schema: {$ref: '#/components/schemas/PublicUlid'}}
    put:
      tags: [Customers]
      security: [{sanctum: []}]
      summary: Replace one nested target discount
      requestBody:
        required: true
        content:
          application/json: {schema: {$ref: '#/components/schemas/CustomerGroupDiscountWriteRequest'}}
      responses:
        '200':
          description: Target discount replaced
          content: {application/json: {schema: {$ref: '#/components/schemas/CustomerGroupDiscountEnvelope'}}}
        '404': {description: Nested discount or same-Store Catalog target not found}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      tags: [Customers]
      security: [{sanctum: []}]
      summary: Delete one nested target discount
      responses:
        '204': {description: Target discount deleted}
        '404': {description: Nested discount not found}
  /graphql:
    post:
      summary: Lighthouse GraphQL endpoint
      description: See graphql/schema.graphql; authentication mutations are not exposed here.
      responses:
        '200': {description: GraphQL response envelope}
components:
  securitySchemes:
    sanctum: {type: http, scheme: bearer}
  parameters:
    Page:
      name: page
      in: query
      schema: {type: integer, minimum: 1, default: 1}
    PerPage:
      name: per_page
      in: query
      schema: {type: integer, minimum: 1, maximum: 100, default: 25}
    StoreId:
      name: X-Store-ID
      in: header
      required: true
      description: Public Store ULID
      schema: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
  schemas:
    CustomerStatus:
      type: string
      enum: [active, disabled]
    CustomerCreditType:
      type: string
      enum: [return, gift, adjustment]
    CustomerGroupCategoryAccess:
      type: string
      enum: [none, all, specific]
    CustomerGroupDiscountTarget:
      type: string
      enum: [category, product]
    CustomerGroupDiscountAppliesTo:
      type: string
      enum: [category_only, category_and_descendants, not_applicable]
    CustomerWriteRequest:
      type: object
      minProperties: 1
      properties:
        email: {type: string, format: email, maxLength: 320}
        customer_group_id: {anyOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        company: {type: [string, 'null'], maxLength: 255}
        first_name: {type: [string, 'null'], maxLength: 100}
        last_name: {type: [string, 'null'], maxLength: 100}
        phone: {type: [string, 'null'], maxLength: 50}
        status: {$ref: '#/components/schemas/CustomerStatus'}
        registered_ip: {type: [string, 'null'], format: ip}
        admin_notes: {type: [string, 'null'], maxLength: 10000}
        points_balance: {type: integer, minimum: 0}
        redeemed_points: {type: integer, minimum: 0}
        joined_at: {type: string, format: date-time}
    CreateCustomerRequest:
      allOf:
        - {$ref: '#/components/schemas/CustomerWriteRequest'}
        - type: object
          required: [email]
    Customer:
      type: object
      required: [id, email, status, points_balance, redeemed_points, credit_balance, joined_at]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        customer_group_id: {anyOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        email: {type: string, format: email}
        company: {type: [string, 'null']}
        first_name: {type: [string, 'null']}
        last_name: {type: [string, 'null']}
        phone: {type: [string, 'null']}
        status: {$ref: '#/components/schemas/CustomerStatus'}
        registered_ip: {type: [string, 'null']}
        admin_notes: {type: [string, 'null']}
        points_balance: {type: integer, minimum: 0}
        redeemed_points: {type: integer, minimum: 0}
        credit_balance: {type: string, pattern: '^-?[0-9]+\.[0-9]{4}$'}
        email_verified_at: {type: [string, 'null'], format: date-time}
        joined_at: {type: string, format: date-time}
        last_activity_at: {type: [string, 'null'], format: date-time}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    CustomerEnvelope:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/Customer'}
    CreateCustomerCreditRequest:
      type: object
      required: [amount, type, reason]
      properties:
        amount: {type: string, pattern: '^-?(?!0+(?:\.0+)?$)[0-9]+(?:\.[0-9]{1,4})?$'}
        type: {$ref: '#/components/schemas/CustomerCreditType'}
        external_reference: {type: [string, 'null'], maxLength: 120}
        reason: {type: string, minLength: 1, maxLength: 200}
        occurred_at: {type: string, format: date-time}
    CustomerCredit:
      type: object
      required: [id, amount, type, reason, occurred_at, created_at]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        amount: {type: string, pattern: '^-?[0-9]+\.[0-9]{4}$'}
        type: {$ref: '#/components/schemas/CustomerCreditType'}
        external_reference: {type: [string, 'null']}
        reason: {type: string}
        occurred_at: {type: string, format: date-time}
        created_by:
          anyOf:
            - type: object
              required: [id, name]
              properties:
                id: {$ref: '#/components/schemas/PublicUlid'}
                name: {type: string}
            - {type: 'null'}
        created_at: {type: string, format: date-time}
    CustomerCreditEnvelope:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/CustomerCredit'}
    CustomerGroupTranslationInput:
      type: object
      required: [language_id, name]
      properties:
        language_id: {$ref: '#/components/schemas/PublicUlid'}
        name: {type: string, minLength: 1, maxLength: 255}
        lock_it: {type: boolean, default: false}
    CustomerGroupTranslationWriteRequest:
      type: object
      required: [name]
      properties:
        name: {type: string, minLength: 1, maxLength: 255}
        lock_it: {type: boolean}
    CustomerGroupTranslation:
      type: object
      required: [id, language, name, lock_it]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        language: {$ref: '#/components/schemas/Language'}
        name: {type: string}
        lock_it: {type: boolean}
    CreateCustomerGroupRequest:
      type: object
      required: [code, discount_method, translations]
      properties:
        code: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]*$', maxLength: 100}
        default_discount_percentage: {type: string, pattern: '^(?:100(?:\.0{1,4})?|[0-9]{1,2}(?:\.[0-9]{1,4})?)$'}
        discount_method: {type: string, minLength: 1, maxLength: 100}
        is_default: {type: boolean, default: false}
        category_access_type: {$ref: '#/components/schemas/CustomerGroupCategoryAccess'}
        category_ids:
          type: array
          maxItems: 500
          uniqueItems: true
          items: {$ref: '#/components/schemas/PublicUlid'}
        translations:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/CustomerGroupTranslationInput'}
    UpdateCustomerGroupRequest:
      type: object
      minProperties: 1
      properties:
        code: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]*$', maxLength: 100}
        default_discount_percentage: {type: string, pattern: '^(?:100(?:\.0{1,4})?|[0-9]{1,2}(?:\.[0-9]{1,4})?)$'}
        discount_method: {type: string, minLength: 1, maxLength: 100}
        is_default: {type: boolean}
        category_access_type: {$ref: '#/components/schemas/CustomerGroupCategoryAccess'}
    ReplaceCustomerGroupCategoriesRequest:
      type: object
      required: [category_ids]
      properties:
        category_ids:
          type: array
          maxItems: 500
          uniqueItems: true
          items: {$ref: '#/components/schemas/PublicUlid'}
    CustomerGroupDiscountWriteRequest:
      type: object
      required: [target_type, target_id, discount_percentage, applies_to, discount_method]
      properties:
        target_type: {$ref: '#/components/schemas/CustomerGroupDiscountTarget'}
        target_id: {$ref: '#/components/schemas/PublicUlid'}
        discount_percentage: {type: string, pattern: '^(?:100(?:\.0{1,4})?|[0-9]{1,2}(?:\.[0-9]{1,4})?)$'}
        applies_to: {$ref: '#/components/schemas/CustomerGroupDiscountAppliesTo'}
        discount_method: {type: string, minLength: 1, maxLength: 100}
    CustomerGroupDiscount:
      type: object
      required: [id, target_type, target_id, discount_percentage, applies_to, discount_method]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        target_type: {$ref: '#/components/schemas/CustomerGroupDiscountTarget'}
        target_id: {$ref: '#/components/schemas/PublicUlid'}
        discount_percentage: {type: string, pattern: '^(?:100(?:\.0{4})?|[0-9]{1,2}\.[0-9]{4})$'}
        applies_to: {$ref: '#/components/schemas/CustomerGroupDiscountAppliesTo'}
        discount_method: {type: string}
    CustomerGroupDiscountEnvelope:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/CustomerGroupDiscount'}
    CustomerGroup:
      type: object
      required: [id, code, default_discount_percentage, discount_method, is_default, category_access_type, category_ids, translations, discounts]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        code: {type: string}
        default_discount_percentage: {type: string}
        discount_method: {type: string}
        is_default: {type: boolean}
        category_access_type: {$ref: '#/components/schemas/CustomerGroupCategoryAccess'}
        customer_count: {type: integer, minimum: 0}
        category_ids:
          type: array
          items: {$ref: '#/components/schemas/PublicUlid'}
        translations:
          type: array
          items: {$ref: '#/components/schemas/CustomerGroupTranslation'}
        discounts:
          type: array
          items: {$ref: '#/components/schemas/CustomerGroupDiscount'}
        translation_request:
          anyOf:
            - {$ref: '#/components/schemas/TranslationRequest'}
            - {type: 'null'}
    CustomerGroupEnvelope:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/CustomerGroup'}
    CreateMediaUploadRequest:
      type: object
      required: [file]
      properties:
        file: {type: string, format: binary}
        disk: {type: string, description: Must be in the deployment allow-list.}
        visibility: {type: string, enum: [private, public], default: private}
        alt_text: {type: [string, 'null'], maxLength: 5000}
        title: {type: [string, 'null'], maxLength: 255}
        caption: {type: [string, 'null'], maxLength: 10000}
        metadata: {type: [object, 'null'], additionalProperties: true}
    AttachVariantMediaRequest:
      type: object
      required: [media_id]
      properties:
        media_id: {$ref: '#/components/schemas/PublicUlid'}
        sort_order: {type: integer, minimum: 0, maximum: 4294967295, default: 0}
    AttachProductMediaRequest:
      allOf:
        - {$ref: '#/components/schemas/AttachVariantMediaRequest'}
        - type: object
          properties:
            is_primary: {type: boolean, default: false}
    MediaVariant:
      type: object
      required: [id, variant, mime_type, size, width, height, content_url]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        variant: {type: string, enum: [original, thumbnail, small, medium, large]}
        mime_type: {type: string}
        size: {type: integer, minimum: 0}
        width: {type: integer, minimum: 1}
        height: {type: integer, minimum: 1}
        metadata: {type: [object, 'null'], additionalProperties: true}
        content_url: {type: string}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    Media:
      type: object
      required: [id, original_filename, filename, mime_type, size, status, visibility, content_url, variants]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        original_filename: {type: string}
        filename: {type: string}
        extension: {type: [string, 'null']}
        mime_type: {type: string}
        size: {type: integer, minimum: 0}
        width: {type: [integer, 'null'], minimum: 1}
        height: {type: [integer, 'null'], minimum: 1}
        duration: {type: [number, string, 'null']}
        checksum: {type: [string, 'null']}
        alt_text: {type: [string, 'null']}
        title: {type: [string, 'null']}
        caption: {type: [string, 'null']}
        status: {type: string, enum: [pending, processing, ready, failed]}
        visibility: {type: string, enum: [private, public]}
        metadata: {type: [object, 'null'], additionalProperties: true}
        content_url: {type: string}
        variants: {type: array, items: {$ref: '#/components/schemas/MediaVariant'}}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    MediaResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/Media'}
    MediaPageResponse:
      type: object
      required: [data, links, meta]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/Media'}}
        links: {$ref: '#/components/schemas/PaginationLinks'}
        meta: {$ref: '#/components/schemas/PaginationMeta'}
    PaginationLinks:
      type: object
      required: [first, last, prev, next]
      properties:
        first: {type: string}
        last: {type: string}
        prev: {type: [string, 'null']}
        next: {type: [string, 'null']}
    PaginationMeta:
      type: object
      required: [current_page, from, last_page, path, per_page, to, total]
      properties:
        current_page: {type: integer, minimum: 1}
        from: {type: [integer, 'null'], minimum: 1}
        last_page: {type: integer, minimum: 1}
        path: {type: string}
        per_page: {type: integer, minimum: 1, maximum: 100}
        to: {type: [integer, 'null'], minimum: 1}
        total: {type: integer, minimum: 0}
    PublicUlid:
      type: string
      pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'
      description: Public ULID; the internal bigint key is never exposed.
    TranslationRequest:
      type: object
      required: [id, status_url, content_type, source_locale, target_locales, status, attempts]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        status_url: {type: string, description: Relative tenant-scoped polling URL; send the same authentication and X-Store-ID header.}
        content_type: {type: string, enum: [brand, category, product_type, product, store_policy]}
        source_locale: {type: string, maxLength: 35}
        target_locales: {type: array, items: {type: string, maxLength: 35}}
        status: {type: string, enum: [pending, processing, completed, failed, superseded, cancelled]}
        attempts: {type: integer, minimum: 0}
        error: {type: [string, 'null'], description: Safe generic message present only after terminal provider failure.}
        queued_at: {type: [string, 'null'], format: date-time}
        started_at: {type: [string, 'null'], format: date-time}
        completed_at: {type: [string, 'null'], format: date-time}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    ProductCommerceFields:
      type: object
      properties:
        sku: {type: string, maxLength: 250, default: ''}
        downloadfile: {type: string, maxLength: 250, default: ''}
        availability: {type: string, maxLength: 250, default: ''}
        price: {type: [number, string], minimum: 0, multipleOf: 0.0001, default: 0}
        costprice: {type: [number, string], minimum: 0, multipleOf: 0.0001, default: 0}
        retailprice: {type: [number, string], minimum: 0, multipleOf: 0.0001, default: 0}
        msrpprice: {type: [number, string], minimum: 0, multipleOf: 0.0001, default: 0}
        saleprice: {type: [number, string], minimum: 0, multipleOf: 0.0001, default: 0}
        calculatedprice: {type: [number, string], minimum: 0, multipleOf: 0.0001, default: 0}
        sortorder: {type: integer, default: 0}
        is_featured: {type: integer, enum: [0, 1], default: 0}
        currentinv: {type: integer, default: 0}
        lowinv: {type: integer, default: 0}
        warranty: {type: [string, 'null']}
        weight: {type: [number, string], minimum: 0, multipleOf: 0.0001, default: 0}
        width: {type: [number, string], minimum: 0, multipleOf: 0.0001, default: 0}
        height: {type: [number, string], minimum: 0, multipleOf: 0.0001, default: 0}
        proddepth: {type: [number, string], minimum: 0, multipleOf: 0.0001, default: 0}
        fixedshippingcost: {type: [number, string], minimum: 0, multipleOf: 0.0001, default: 0}
        freeshipping: {type: integer, enum: [0, 1], default: 0}
        ratingtotal: {type: integer, minimum: 0, default: 0}
        numratings: {type: integer, minimum: 0, default: 0}
        numsold: {type: integer, minimum: 0, default: 0}
        numviews: {type: integer, minimum: 0, default: 0}
        allowpurchases: {type: integer, enum: [0, 1], default: 1}
        hideprice: {type: integer, enum: [0, 1], default: 0}
        is_login_for_price: {type: integer, enum: [0, 1], default: 0}
        is_global_search: {type: integer, enum: [0, 1], default: 0}
        condition: {type: string, enum: [New, Used, Refurbished], default: New}
        showcondition: {type: integer, enum: [0, 1], default: 0}
        pre_order: {type: integer, enum: [0, 1], default: 0}
        releasedate: {type: [string, 'null'], format: date-time}
        releasedateremove: {type: integer, enum: [0, 1], default: 0}
        minqty: {type: integer, minimum: 0, default: 0}
        maxqty: {type: integer, minimum: 0, default: 0}
        tax_class_id: {type: integer, minimum: 0, default: 0}
        show_related_product: {type: integer, minimum: 0, default: 0, description: Number of related Products to display; zero disables the block.}
        prodpoints: {type: integer, minimum: 0, default: 0}
        reviews_on: {type: integer, enum: [0, 1], default: 0}
        upc: {type: [string, 'null'], maxLength: 32, default: ''}
        hs_code: {type: [string, 'null'], maxLength: 32, default: ''}
        gtin: {type: [string, 'null'], maxLength: 32, default: ''}
        mpn: {type: [string, 'null'], maxLength: 32, default: ''}
        bpn: {type: [string, 'null'], maxLength: 32, default: ''}
    CollectionTranslationWrite:
      type: object
      required: [locale, title, slug]
      properties:
        locale: {type: string, maxLength: 35}
        title: {type: string, maxLength: 255}
        slug: {type: string, maxLength: 255}
        description: {type: [string, 'null']}
        seo_title: {type: [string, 'null'], maxLength: 255}
        seo_description: {type: [string, 'null']}
        lock_it: {type: boolean, default: false}
    CollectionRuleWrite:
      type: object
      required: [field, operator, value]
      properties:
        field: {type: string, enum: [vendor, sku, status, product_type, title, brand, tag, price]}
        operator: {type: string, enum: [equals, not_equals, contains, greater_than, greater_than_or_equal, less_than, less_than_or_equal]}
        value: {type: string, maxLength: 255}
        position: {type: integer, minimum: 0, maximum: 4294967295}
    CollectionProductWrite:
      type: object
      required: [product_id]
      properties:
        product_id: {$ref: '#/components/schemas/PublicUlid'}
        sort_order: {type: integer, minimum: 0, maximum: 4294967295}
        is_pinned: {type: boolean, default: false}
    CollectionWriteRequest:
      type: object
      properties:
        parent_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        image_url: {type: [string, 'null'], maxLength: 500}
        is_active: {type: boolean, default: true}
        sort_order: {type: integer, minimum: 0, maximum: 4294967295, default: 0}
        collection_type: {type: string, enum: [manual, rule_based, ai_generated], default: manual}
        rules_match_type: {type: string, enum: [all, any], default: all}
        ai_prompt: {type: [string, 'null'], maxLength: 20000}
        ai_model: {type: [string, 'null'], maxLength: 100}
        translations:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/CollectionTranslationWrite'}
        rules:
          type: array
          maxItems: 100
          items: {$ref: '#/components/schemas/CollectionRuleWrite'}
        products:
          type: array
          maxItems: 1000
          items: {$ref: '#/components/schemas/CollectionProductWrite'}
    CreateCollectionRequest:
      allOf:
        - {$ref: '#/components/schemas/CollectionWriteRequest'}
        - type: object
          required: [translations]
    CollectionRuleReplacementRequest:
      type: object
      required: [rules]
      properties:
        rules:
          type: array
          maxItems: 100
          items: {$ref: '#/components/schemas/CollectionRuleWrite'}
    CollectionProductReplacementRequest:
      type: object
      required: [products]
      properties:
        products:
          type: array
          maxItems: 1000
          uniqueItems: true
          items: {$ref: '#/components/schemas/CollectionProductWrite'}
    CollectionRule:
      allOf:
        - {$ref: '#/components/schemas/CollectionRuleWrite'}
        - type: object
          required: [id, position]
          properties:
            id: {$ref: '#/components/schemas/PublicUlid'}
            created_at: {type: [string, 'null'], format: date-time}
            updated_at: {type: [string, 'null'], format: date-time}
    CollectionAiJob:
      type: object
      required: [id, prompt, model, status]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        prompt: {type: string}
        model: {type: string}
        status: {type: string, enum: [pending, processing, completed, failed]}
        result_rules: {type: [array, 'null'], items: {type: object, additionalProperties: true}}
        matched_count: {type: [integer, 'null'], minimum: 0}
        error_message: {type: [string, 'null']}
        tokens_used: {type: [integer, 'null'], minimum: 0}
        completed_at: {type: [string, 'null'], format: date-time}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    CatalogCollection:
      type: object
      required: [id, is_active, sort_order, collection_type, rules_match_type, translations, rules]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        parent_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        image_url: {type: [string, 'null']}
        is_active: {type: boolean}
        sort_order: {type: integer, minimum: 0}
        collection_type: {type: string, enum: [manual, rule_based, ai_generated]}
        rules_match_type: {type: string, enum: [all, any]}
        ai_prompt: {type: [string, 'null']}
        ai_model: {type: [string, 'null']}
        ai_status: {type: [string, 'null'], enum: [pending, processing, completed, failed, null]}
        ai_last_run_at: {type: [string, 'null'], format: date-time}
        ai_error_message: {type: [string, 'null']}
        children_count: {type: integer, minimum: 0}
        products_count: {type: integer, minimum: 0}
        rules_count: {type: integer, minimum: 0}
        ai_jobs_count: {type: integer, minimum: 0}
        translations: {type: array, items: {$ref: '#/components/schemas/CollectionTranslationWrite'}}
        rules: {type: array, items: {$ref: '#/components/schemas/CollectionRule'}}
        translation_request:
          oneOf:
            - {$ref: '#/components/schemas/TranslationRequest'}
            - {type: 'null'}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    ProductTranslationWrite:
      type: object
      required: [locale, title, slug]
      properties:
        locale: {type: string, maxLength: 35}
        title: {type: string, maxLength: 255}
        slug: {type: string, maxLength: 255}
        description: {type: [string, 'null']}
        seo_title: {type: [string, 'null'], maxLength: 255}
        seo_description: {type: [string, 'null']}
        lock_it: {type: boolean, default: false}
    ProductWriteRequest:
      allOf:
        - {$ref: '#/components/schemas/ProductCommerceFields'}
        - type: object
          properties:
            brand_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
            platform_taxonomy_node_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
            vendor: {type: [string, 'null'], maxLength: 255}
            product_type_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
            fulfillment_type: {type: string, enum: [physical, digital, software, service], default: physical}
            track_inventory: {type: boolean, default: true}
            status: {type: string, enum: [draft, active, archived], default: draft}
            category_ids: {type: array, maxItems: 100, uniqueItems: true, items: {$ref: '#/components/schemas/PublicUlid'}}
            primary_category_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
            translations:
              type: array
              minItems: 1
              items: {$ref: '#/components/schemas/ProductTranslationWrite'}
    CreateProductRequest:
      allOf:
        - {$ref: '#/components/schemas/ProductWriteRequest'}
        - type: object
          required: [translations]
    UpdateProductRequest:
      allOf:
        - {$ref: '#/components/schemas/ProductWriteRequest'}
        - type: object
          minProperties: 1
    ProductDetailLocalReference:
      type: string
      pattern: '^[A-Za-z0-9_.-]{1,100}$'
      description: Request-local name returned as a public ULID and referenced later in the same command with an @ prefix.
    ProductDetailPublicIdOrReference:
      type: string
      pattern: '^(?:[0-9A-HJKMNP-TV-Z]{26}|@[A-Za-z0-9_.-]{1,100})$'
    ProductDetailItemCommand:
      type: object
      description: Section-specific REST write fields; id updates an existing resource, while omitted id creates one. ref names a newly created resource for later items in the command.
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        ref: {$ref: '#/components/schemas/ProductDetailLocalReference'}
      additionalProperties: true
    ProductDetailUpsertSection:
      type: object
      properties:
        upsert:
          type: array
          items: {$ref: '#/components/schemas/ProductDetailItemCommand'}
        delete:
          type: array
          uniqueItems: true
          items: {$ref: '#/components/schemas/PublicUlid'}
    ProductDetailOptionsSection:
      allOf:
        - {$ref: '#/components/schemas/ProductDetailUpsertSection'}
        - type: object
          properties:
            value_upsert:
              type: array
              items: {$ref: '#/components/schemas/ProductDetailItemCommand'}
            value_delete:
              type: array
              items:
                type: object
                required: [option_id, id]
                properties:
                  option_id: {$ref: '#/components/schemas/PublicUlid'}
                  id: {$ref: '#/components/schemas/PublicUlid'}
    ProductDetailCustomFieldSection:
      type: object
      properties:
        upsert:
          type: array
          items:
            allOf:
              - {$ref: '#/components/schemas/ProductDetailItemCommand'}
              - type: object
                required: [definition_id]
                properties:
                  definition_id: {$ref: '#/components/schemas/PublicUlid'}
                  variant_id:
                    oneOf:
                      - {$ref: '#/components/schemas/ProductDetailPublicIdOrReference'}
                      - {type: 'null'}
        delete:
          type: array
          items:
            type: object
            required: [definition_id]
            properties:
              definition_id: {$ref: '#/components/schemas/PublicUlid'}
              variant_id:
                oneOf:
                  - {$ref: '#/components/schemas/PublicUlid'}
                  - {type: 'null'}
      additionalProperties: false
    ProductDetailMediaSection:
      type: object
      properties:
        attach: {type: array, items: {type: object, required: [media_id], properties: {media_id: {$ref: '#/components/schemas/PublicUlid'}, sort_order: {type: integer, minimum: 0}, is_primary: {type: boolean}}}}
        detach: {type: array, uniqueItems: true, items: {$ref: '#/components/schemas/PublicUlid'}}
        variant_attach: {type: array, items: {type: object, required: [variant_id, media_id], properties: {variant_id: {$ref: '#/components/schemas/ProductDetailPublicIdOrReference'}, media_id: {$ref: '#/components/schemas/PublicUlid'}, sort_order: {type: integer, minimum: 0}}}}
        variant_detach: {type: array, items: {type: object, required: [variant_id, media_id], properties: {variant_id: {$ref: '#/components/schemas/ProductDetailPublicIdOrReference'}, media_id: {$ref: '#/components/schemas/PublicUlid'}}}}
        primary_media_id: {$ref: '#/components/schemas/PublicUlid'}
      additionalProperties: false
    ProductDetailSectionsWrite:
      type: object
      description: Catalog built-ins plus explicitly registered Product Detail section-provider commands.
      properties:
        images: {$ref: '#/components/schemas/ProductDetailUpsertSection'}
        media: {$ref: '#/components/schemas/ProductDetailMediaSection'}
        custom_fields: {$ref: '#/components/schemas/ProductDetailCustomFieldSection'}
        options: {$ref: '#/components/schemas/ProductDetailOptionsSection'}
        variants: {$ref: '#/components/schemas/ProductDetailUpsertSection'}
        shared_options: {$ref: '#/components/schemas/ProductDetailUpsertSection'}
        modifier_groups: {$ref: '#/components/schemas/ProductDetailUpsertSection'}
        modifiers: {$ref: '#/components/schemas/ProductDetailUpsertSection'}
      additionalProperties:
        type: object
        description: Command validated and saved by the owning registered module provider.
        additionalProperties: true
    ProductDetailWriteRequest:
      type: object
      properties:
        expected_updated_at: {type: [string, 'null'], format: date-time, description: Optional revision from the latest Product Detail read; mismatch returns 409.}
        product: {$ref: '#/components/schemas/ProductWriteRequest'}
        sections: {$ref: '#/components/schemas/ProductDetailSectionsWrite'}
      additionalProperties: false
    CreateProductDetailRequest:
      allOf:
        - {$ref: '#/components/schemas/ProductDetailWriteRequest'}
        - type: object
          required: [product]
    UpdateProductDetailRequest:
      allOf:
        - {$ref: '#/components/schemas/ProductDetailWriteRequest'}
        - type: object
          minProperties: 1
    ProductDetailSectionMeta:
      type: object
      required: [total, returned, limit, truncated]
      properties:
        total: {type: integer, minimum: 0}
        returned: {type: integer, minimum: 0}
        limit: {type: integer, minimum: 1, maximum: 250}
        truncated: {type: boolean}
    ProductDetailData:
      type: object
      required: [product, revision, sections, section_meta, capabilities]
      properties:
        product:
          oneOf:
            - {$ref: '#/components/schemas/Product'}
            - {type: 'null'}
        revision: {type: [string, 'null'], format: date-time}
        sections:
          type: object
          description: Selected Catalog built-ins plus serialization-ready data from selected registered providers. All sections are returned when the sections query parameter is omitted.
          properties:
            images: {type: array, items: {$ref: '#/components/schemas/ProductImage'}}
            media: {type: array, items: {type: object, additionalProperties: true}}
            custom_fields: {type: array, items: {$ref: '#/components/schemas/ProductCustomFieldValue'}}
            options: {type: array, items: {$ref: '#/components/schemas/ProductOption'}}
            variants: {type: array, items: {$ref: '#/components/schemas/ProductVariant'}}
            shared_options: {type: array, items: {$ref: '#/components/schemas/ProductSharedOptionAssignment'}}
            modifier_groups: {type: array, items: {type: object, additionalProperties: true}}
            modifiers: {type: array, items: {type: object, additionalProperties: true}}
          additionalProperties: true
        section_meta:
          type: object
          additionalProperties: {$ref: '#/components/schemas/ProductDetailSectionMeta'}
        reference_data: {type: object, additionalProperties: true}
        capabilities: {type: object, additionalProperties: true}
        saved_sections: {type: array, uniqueItems: true, items: {type: string}}
        references:
          type: object
          additionalProperties:
            type: object
            additionalProperties: {$ref: '#/components/schemas/PublicUlid'}
    ProductDetailResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ProductDetailData'}
    ProductTranslation:
      allOf:
        - {$ref: '#/components/schemas/ProductTranslationWrite'}
        - type: object
          properties:
            created_at: {type: [string, 'null'], format: date-time}
            updated_at: {type: [string, 'null'], format: date-time}
    ProductCategoryReference:
      type: object
      required: [id, sort_order, is_primary]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        sort_order: {type: integer, minimum: 0}
        is_primary: {type: boolean}
    Product:
      allOf:
        - {$ref: '#/components/schemas/ProductCommerceFields'}
        - type: object
          required: [id, fulfillment_type, track_inventory, status, has_variants, categories, translations]
          properties:
            id: {$ref: '#/components/schemas/PublicUlid'}
            brand_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
            platform_taxonomy_node_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
            vendor: {type: [string, 'null']}
            product_type_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
            fulfillment_type: {type: string, enum: [physical, digital, software, service]}
            track_inventory: {type: boolean}
            status: {type: string, enum: [draft, active, archived]}
            has_variants: {type: boolean, readOnly: true}
            published_at: {type: [string, 'null'], format: date-time}
            primary_category_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
            categories_count: {type: integer, minimum: 0}
            options_count: {type: integer, minimum: 0}
            variants_count: {type: integer, minimum: 0}
            categories: {type: array, items: {$ref: '#/components/schemas/ProductCategoryReference'}}
            translations: {type: array, items: {$ref: '#/components/schemas/ProductTranslation'}}
            options: {type: array, items: {$ref: '#/components/schemas/ProductOption'}}
            variants: {type: array, items: {$ref: '#/components/schemas/ProductVariant'}}
            translation_request:
              oneOf:
                - {$ref: '#/components/schemas/TranslationRequest'}
                - {type: 'null'}
            created_at: {type: [string, 'null'], format: date-time}
            updated_at: {type: [string, 'null'], format: date-time}
    ProductResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/Product'}
    ProductPageResponse:
      type: object
      required: [data, links, meta]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/Product'}}
        links: {$ref: '#/components/schemas/PaginationLinks'}
        meta: {$ref: '#/components/schemas/PaginationMeta'}
    CustomFieldType:
      type: string
      enum: [text, number, boolean, select, multi_select, date, url]
    CustomFieldDefinitionTranslationWrite:
      type: object
      required: [locale, label]
      properties:
        locale: {type: string, maxLength: 35}
        label: {type: string, maxLength: 255}
        help_text: {type: [string, 'null']}
        lock_it: {type: boolean, default: false}
    CustomFieldOptionTranslationWrite:
      type: object
      required: [locale, label]
      properties:
        locale: {type: string, maxLength: 35}
        label: {type: string, maxLength: 255}
        lock_it: {type: boolean, default: false}
    CustomFieldOptionWrite:
      type: object
      properties:
        position: {type: integer, minimum: 0, maximum: 4294967295, default: 0}
        translations:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/CustomFieldOptionTranslationWrite'}
    CreateCustomFieldOptionRequest:
      allOf:
        - {$ref: '#/components/schemas/CustomFieldOptionWrite'}
        - type: object
          required: [translations]
    UpdateCustomFieldOptionRequest:
      allOf:
        - {$ref: '#/components/schemas/CustomFieldOptionWrite'}
        - type: object
          minProperties: 1
    CustomFieldDefinitionWrite:
      type: object
      properties:
        product_type: {type: [string, 'null'], maxLength: 255, description: Nullable Product Type code; null makes the definition global.}
        field_key: {type: string, maxLength: 100, pattern: '^[A-Za-z][A-Za-z0-9_.-]*$'}
        field_type: {$ref: '#/components/schemas/CustomFieldType'}
        is_required: {type: boolean, default: false}
        is_filterable: {type: boolean, default: false}
        position: {type: integer, minimum: 0, maximum: 4294967295, default: 0}
        translations:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/CustomFieldDefinitionTranslationWrite'}
    CreateCustomFieldDefinitionRequest:
      allOf:
        - {$ref: '#/components/schemas/CustomFieldDefinitionWrite'}
        - type: object
          required: [field_key, field_type, translations]
          properties:
            options:
              type: array
              maxItems: 500
              items: {$ref: '#/components/schemas/CreateCustomFieldOptionRequest'}
    UpdateCustomFieldDefinitionRequest:
      allOf:
        - {$ref: '#/components/schemas/CustomFieldDefinitionWrite'}
        - type: object
          minProperties: 1
    CustomFieldDefinitionTranslation:
      allOf:
        - {$ref: '#/components/schemas/CustomFieldDefinitionTranslationWrite'}
        - type: object
          required: [lock_it]
          properties:
            created_at: {type: [string, 'null'], format: date-time}
            updated_at: {type: [string, 'null'], format: date-time}
    CustomFieldOptionTranslation:
      allOf:
        - {$ref: '#/components/schemas/CustomFieldOptionTranslationWrite'}
        - type: object
          required: [lock_it]
          properties:
            created_at: {type: [string, 'null'], format: date-time}
            updated_at: {type: [string, 'null'], format: date-time}
    CustomFieldOption:
      type: object
      required: [id, position, translations]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        definition_id: {$ref: '#/components/schemas/PublicUlid'}
        position: {type: integer, minimum: 0}
        translations: {type: array, items: {$ref: '#/components/schemas/CustomFieldOptionTranslation'}}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    CustomFieldDefinition:
      type: object
      required: [id, field_key, field_type, is_required, is_filterable, position, translations, options]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        product_type: {type: [string, 'null']}
        field_key: {type: string}
        field_type: {$ref: '#/components/schemas/CustomFieldType'}
        is_required: {type: boolean}
        is_filterable: {type: boolean}
        position: {type: integer, minimum: 0}
        values_count: {type: integer, minimum: 0}
        translations: {type: array, items: {$ref: '#/components/schemas/CustomFieldDefinitionTranslation'}}
        options: {type: array, items: {$ref: '#/components/schemas/CustomFieldOption'}}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    CustomFieldDefinitionResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/CustomFieldDefinition'}
    CustomFieldDefinitionPageResponse:
      type: object
      required: [data, links, meta]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/CustomFieldDefinition'}}
        links: {type: object, additionalProperties: true}
        meta: {$ref: '#/components/schemas/PaginationMeta'}
    CustomFieldOptionResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/CustomFieldOption'}
    CustomFieldOptionListResponse:
      type: object
      required: [data]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/CustomFieldOption'}}
    ProductCustomFieldValueTranslationWrite:
      type: object
      required: [locale, value_text]
      properties:
        locale: {type: string, maxLength: 35}
        value_text: {type: string}
        lock_it: {type: boolean, default: false}
    ProductCustomFieldValueTranslation:
      allOf:
        - {$ref: '#/components/schemas/ProductCustomFieldValueTranslationWrite'}
        - type: object
          required: [lock_it]
          properties:
            created_at: {type: [string, 'null'], format: date-time}
            updated_at: {type: [string, 'null'], format: date-time}
    ProductCustomFieldValueRequest:
      type: object
      minProperties: 1
      maxProperties: 1
      description: Supply exactly one property matching the definition type; translations serves text/url, option_id serves select, and option_ids serves multi_select.
      properties:
        value_number: {type: number, multipleOf: 0.0001}
        value_boolean: {type: boolean}
        value_date: {type: string, format: date}
        option_id: {$ref: '#/components/schemas/PublicUlid'}
        option_ids:
          type: array
          minItems: 1
          maxItems: 500
          uniqueItems: true
          items: {$ref: '#/components/schemas/PublicUlid'}
        translations:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/ProductCustomFieldValueTranslationWrite'}
    ProductCustomFieldValue:
      type: object
      required: [id, product_id, definition_id, definition, translations, selected_options]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        product_id: {$ref: '#/components/schemas/PublicUlid'}
        variant_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        definition_id: {$ref: '#/components/schemas/PublicUlid'}
        definition: {$ref: '#/components/schemas/CustomFieldDefinition'}
        value_number: {type: [string, 'null'], description: Exact four-decimal serialization.}
        value_boolean: {type: [boolean, 'null']}
        value_date: {type: [string, 'null'], format: date}
        option_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        selected_option: {oneOf: [{$ref: '#/components/schemas/CustomFieldOption'}, {type: 'null'}]}
        option_ids: {type: array, items: {$ref: '#/components/schemas/PublicUlid'}}
        selected_options: {type: array, items: {$ref: '#/components/schemas/CustomFieldOption'}}
        translations: {type: array, items: {$ref: '#/components/schemas/ProductCustomFieldValueTranslation'}}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    ProductCustomFieldValueResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ProductCustomFieldValue'}
    ProductCustomFieldValueListResponse:
      type: object
      required: [data]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/ProductCustomFieldValue'}}
    SharedProductOptionType:
      type: string
      enum: [dropdown, radio_buttons, buttons, swatches]
    SharedProductOptionTranslationWrite:
      type: object
      required: [locale, display_name]
      properties:
        locale: {type: string, maxLength: 35}
        display_name: {type: string, minLength: 1, maxLength: 100}
        lock_it: {type: boolean, default: false}
    SharedProductOptionValueTranslationWrite:
      type: object
      required: [locale, display_label]
      properties:
        locale: {type: string, maxLength: 35}
        display_label: {type: string, minLength: 1, maxLength: 100}
        lock_it: {type: boolean, default: false}
    SharedProductOptionValueWrite:
      type: object
      required: [translations]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        position: {type: integer, minimum: 0, maximum: 4294967295}
        is_default: {type: boolean, default: false}
        translations:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/SharedProductOptionValueTranslationWrite'}
    SharedProductOptionWrite:
      type: object
      properties:
        name: {type: string, minLength: 1, maxLength: 100}
        type: {$ref: '#/components/schemas/SharedProductOptionType'}
        position: {type: integer, minimum: 0, maximum: 4294967295}
        translations:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/SharedProductOptionTranslationWrite'}
        values:
          type: array
          minItems: 1
          maxItems: 100
          items: {$ref: '#/components/schemas/SharedProductOptionValueWrite'}
    CreateSharedProductOptionRequest:
      allOf:
        - {$ref: '#/components/schemas/SharedProductOptionWrite'}
        - type: object
          required: [name, type, translations, values]
    UpdateSharedProductOptionRequest:
      allOf:
        - {$ref: '#/components/schemas/SharedProductOptionWrite'}
        - type: object
          minProperties: 1
    SharedProductOptionTranslation:
      allOf:
        - {$ref: '#/components/schemas/SharedProductOptionTranslationWrite'}
        - type: object
          required: [lock_it]
          properties:
            created_at: {type: [string, 'null'], format: date-time}
            updated_at: {type: [string, 'null'], format: date-time}
    SharedProductOptionValueTranslation:
      allOf:
        - {$ref: '#/components/schemas/SharedProductOptionValueTranslationWrite'}
        - type: object
          required: [lock_it]
          properties:
            created_at: {type: [string, 'null'], format: date-time}
            updated_at: {type: [string, 'null'], format: date-time}
    SharedProductOptionValue:
      type: object
      required: [id, position, is_default, translations]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        position: {type: integer, minimum: 0}
        is_default: {type: boolean}
        translations: {type: array, items: {$ref: '#/components/schemas/SharedProductOptionValueTranslation'}}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    SharedProductOption:
      type: object
      required: [id, name, type, position, translations, values]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        name: {type: string, maxLength: 100}
        type: {$ref: '#/components/schemas/SharedProductOptionType'}
        position: {type: integer, minimum: 0}
        products_count: {type: integer, minimum: 0}
        translations: {type: array, items: {$ref: '#/components/schemas/SharedProductOptionTranslation'}}
        values: {type: array, items: {$ref: '#/components/schemas/SharedProductOptionValue'}}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    SharedProductOptionResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/SharedProductOption'}
    SharedProductOptionPageResponse:
      type: object
      required: [data, links, meta]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/SharedProductOption'}}
        links: {$ref: '#/components/schemas/PaginationLinks'}
        meta: {$ref: '#/components/schemas/PaginationMeta'}
    CreateProductSharedOptionAssignmentRequest:
      type: object
      required: [option_id]
      properties:
        option_id: {$ref: '#/components/schemas/PublicUlid'}
        position: {type: integer, minimum: 0, maximum: 4294967295}
    ProductSharedOptionAssignment:
      type: object
      required: [id, position, product_id, option]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        position: {type: integer, minimum: 0}
        product_id: {$ref: '#/components/schemas/PublicUlid'}
        option: {$ref: '#/components/schemas/SharedProductOption'}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    ProductSharedOptionAssignmentResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ProductSharedOptionAssignment'}
    ProductSharedOptionAssignmentListResponse:
      type: object
      required: [data]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/ProductSharedOptionAssignment'}}
    ProductOptionTranslationWrite:
      type: object
      required: [locale, name]
      properties:
        locale: {type: string, maxLength: 35}
        name: {type: string, maxLength: 100}
        lock_it: {type: boolean, default: false}
    ProductOptionValueTranslationWrite:
      type: object
      required: [locale, value]
      properties:
        locale: {type: string, maxLength: 35}
        value: {type: string, maxLength: 100}
        lock_it: {type: boolean, default: false}
    ProductOptionValueWriteRequest:
      type: object
      properties:
        position: {type: integer, minimum: 0, maximum: 4294967295, default: 0}
        translations:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/ProductOptionValueTranslationWrite'}
    CreateProductOptionValueRequest:
      allOf:
        - {$ref: '#/components/schemas/ProductOptionValueWriteRequest'}
        - type: object
          required: [translations]
    UpdateProductOptionValueRequest:
      allOf:
        - {$ref: '#/components/schemas/ProductOptionValueWriteRequest'}
        - type: object
          minProperties: 1
    ProductOptionWriteRequest:
      type: object
      properties:
        position: {type: integer, minimum: 0, maximum: 4294967295, default: 0}
        translations:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/ProductOptionTranslationWrite'}
        values:
          type: array
          maxItems: 100
          items: {$ref: '#/components/schemas/CreateProductOptionValueRequest'}
    CreateProductOptionRequest:
      allOf:
        - {$ref: '#/components/schemas/ProductOptionWriteRequest'}
        - type: object
          required: [translations]
    UpdateProductOptionRequest:
      type: object
      minProperties: 1
      properties:
        position: {type: integer, minimum: 0, maximum: 4294967295}
        translations:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/ProductOptionTranslationWrite'}
    ProductOptionTranslation:
      allOf:
        - {$ref: '#/components/schemas/ProductOptionTranslationWrite'}
        - type: object
          required: [lock_it]
          properties:
            created_at: {type: [string, 'null'], format: date-time}
            updated_at: {type: [string, 'null'], format: date-time}
    ProductOptionValueTranslation:
      allOf:
        - {$ref: '#/components/schemas/ProductOptionValueTranslationWrite'}
        - type: object
          required: [lock_it]
          properties:
            created_at: {type: [string, 'null'], format: date-time}
            updated_at: {type: [string, 'null'], format: date-time}
    ProductOptionValue:
      type: object
      required: [id, position, translations]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        product_id: {$ref: '#/components/schemas/PublicUlid'}
        option_id: {$ref: '#/components/schemas/PublicUlid'}
        position: {type: integer, minimum: 0}
        option_translations: {type: array, items: {$ref: '#/components/schemas/ProductOptionTranslationWrite'}}
        translations: {type: array, items: {$ref: '#/components/schemas/ProductOptionValueTranslation'}}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    ProductOption:
      type: object
      required: [id, position, translations, values]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        product_id: {$ref: '#/components/schemas/PublicUlid'}
        position: {type: integer, minimum: 0}
        translations: {type: array, items: {$ref: '#/components/schemas/ProductOptionTranslation'}}
        values: {type: array, items: {$ref: '#/components/schemas/ProductOptionValue'}}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    ProductOptionResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ProductOption'}
    ProductOptionListResponse:
      type: object
      required: [data]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/ProductOption'}}
    ProductOptionValueResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ProductOptionValue'}
    ProductOptionValueListResponse:
      type: object
      required: [data]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/ProductOptionValue'}}
    ProductVariantTranslationWrite:
      type: object
      required: [locale]
      properties:
        locale: {type: string, maxLength: 35}
        title: {type: [string, 'null'], maxLength: 255}
        lock_it: {type: boolean, default: false}
    ProductVariantWriteRequest:
      type: object
      properties:
        sku: {type: [string, 'null'], maxLength: 100}
        barcode: {type: [string, 'null'], maxLength: 100}
        price_amount_minor: {type: integer, format: int64, minimum: 0}
        compare_at_price_amount_minor: {type: [integer, 'null'], format: int64, minimum: 0}
        msrp_amount_minor: {type: [integer, 'null'], format: int64, minimum: 0}
        cost_per_item_amount_minor: {type: [integer, 'null'], format: int64, minimum: 0}
        currency_code: {type: string, pattern: '^[A-Z]{3}$'}
        inventory_qty: {type: integer, default: 0}
        inventory_policy: {type: string, enum: [deny, continue], default: deny}
        weight_grams: {type: [integer, 'null'], minimum: 0}
        height: {type: [number, 'null'], format: decimal}
        width: {type: [number, 'null'], format: decimal}
        depth: {type: [number, 'null'], format: decimal}
        dimension_unit: {type: string, maxLength: 10, default: cm}
        requires_shipping: {type: boolean, default: true}
        taxable: {type: boolean, default: true}
        call_for_price: {type: boolean, default: false}
        image_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        position: {type: integer, minimum: 0, maximum: 4294967295, default: 0}
        option_value_ids:
          type: array
          maxItems: 100
          uniqueItems: true
          items: {$ref: '#/components/schemas/PublicUlid'}
        translations:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/ProductVariantTranslationWrite'}
    CreateProductVariantRequest:
      allOf:
        - {$ref: '#/components/schemas/ProductVariantWriteRequest'}
        - type: object
          required: [price_amount_minor, currency_code]
    UpdateProductVariantRequest:
      allOf:
        - {$ref: '#/components/schemas/ProductVariantWriteRequest'}
        - type: object
          minProperties: 1
    ProductVariantTranslation:
      allOf:
        - {$ref: '#/components/schemas/ProductVariantTranslationWrite'}
        - type: object
          required: [lock_it]
          properties:
            created_at: {type: [string, 'null'], format: date-time}
            updated_at: {type: [string, 'null'], format: date-time}
    ProductVariant:
      type: object
      required: [id, price_amount_minor, currency_code, inventory_qty, inventory_policy, requires_shipping, taxable, call_for_price, position, translations, option_values]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        product_id: {$ref: '#/components/schemas/PublicUlid'}
        sku: {type: [string, 'null']}
        barcode: {type: [string, 'null']}
        price_amount_minor: {type: integer, format: int64, minimum: 0}
        compare_at_price_amount_minor: {type: [integer, 'null'], format: int64, minimum: 0}
        msrp_amount_minor: {type: [integer, 'null'], format: int64, minimum: 0}
        cost_per_item_amount_minor: {type: [integer, 'null'], format: int64, minimum: 0}
        currency_code: {type: string, pattern: '^[A-Z]{3}$'}
        inventory_qty: {type: integer}
        inventory_policy: {type: string, enum: [deny, continue]}
        weight_grams: {type: [integer, 'null'], minimum: 0}
        height: {type: [string, 'null']}
        width: {type: [string, 'null']}
        depth: {type: [string, 'null']}
        dimension_unit: {type: string}
        requires_shipping: {type: boolean}
        taxable: {type: boolean}
        call_for_price: {type: boolean}
        image_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        position: {type: integer, minimum: 0}
        translations: {type: array, items: {$ref: '#/components/schemas/ProductVariantTranslation'}}
        option_values: {type: array, items: {$ref: '#/components/schemas/ProductOptionValue'}}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    ProductVariantResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ProductVariant'}
    ProductVariantPageResponse:
      type: object
      required: [data, links, meta]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/ProductVariant'}}
        links: {$ref: '#/components/schemas/PaginationLinks'}
        meta: {$ref: '#/components/schemas/PaginationMeta'}
    ProductImageTranslationWrite:
      type: object
      required: [locale]
      properties:
        locale: {type: string, maxLength: 35}
        alt_text: {type: [string, 'null'], maxLength: 255}
        lock_it: {type: boolean, default: false}
    ProductImageWriteRequest:
      type: object
      properties:
        variant_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        url: {type: string, maxLength: 500, pattern: '^(?:/|https?://)', description: Root-relative or HTTP(S) image locator.}
        width: {type: [integer, 'null'], minimum: 1, maximum: 4294967295}
        height: {type: [integer, 'null'], minimum: 1, maximum: 4294967295}
        position: {type: integer, minimum: 0, maximum: 4294967295, default: 0}
        translations:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/ProductImageTranslationWrite'}
    CreateProductImageRequest:
      allOf:
        - {$ref: '#/components/schemas/ProductImageWriteRequest'}
        - type: object
          required: [url]
    UpdateProductImageRequest:
      allOf:
        - {$ref: '#/components/schemas/ProductImageWriteRequest'}
        - type: object
          minProperties: 1
    ProductImageTranslation:
      allOf:
        - {$ref: '#/components/schemas/ProductImageTranslationWrite'}
        - type: object
          required: [lock_it]
          properties:
            created_at: {type: [string, 'null'], format: date-time}
            updated_at: {type: [string, 'null'], format: date-time}
    ProductImage:
      type: object
      required: [id, product_id, url, position, translations]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        product_id: {$ref: '#/components/schemas/PublicUlid'}
        variant_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        url: {type: string, maxLength: 500}
        width: {type: [integer, 'null'], minimum: 1}
        height: {type: [integer, 'null'], minimum: 1}
        position: {type: integer, minimum: 0}
        translations: {type: array, items: {$ref: '#/components/schemas/ProductImageTranslation'}}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    ProductImageResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/ProductImage'}
    ProductImagePageResponse:
      type: object
      required: [data, links, meta]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/ProductImage'}}
        links: {$ref: '#/components/schemas/PaginationLinks'}
        meta: {$ref: '#/components/schemas/PaginationMeta'}
    BrandTranslationWrite:
      type: object
      required: [locale, name, slug]
      properties:
        locale: {type: string, maxLength: 35}
        name: {type: string, maxLength: 255}
        slug: {type: string, maxLength: 255}
        description: {type: [string, 'null']}
        seo_title: {type: [string, 'null'], maxLength: 255}
        seo_description: {type: [string, 'null']}
        lock_it: {type: boolean, default: false, description: Prevent automated translation writers from overwriting this merchant-authored record.}
    CreateBrandRequest:
      type: object
      required: [translations]
      properties:
        image: {type: [string, 'null'], format: binary, description: Managed JPEG, PNG, WebP, or AVIF Brand image up to 5 MiB; null clears it.}
        banner: {type: [string, 'null'], format: binary, description: Managed JPEG, PNG, WebP, or AVIF Brand banner up to 10 MiB; null clears it.}
        logo_url: {type: [string, 'null'], maxLength: 2048, description: Legacy root-relative or HTTP(S) logo locator.}
        website_url: {type: [string, 'null'], format: uri, maxLength: 2048, description: HTTP(S) official website.}
        origin: {type: [string, 'null'], maxLength: 120}
        is_active: {type: boolean, default: true}
        sort_order: {type: integer, minimum: 0, default: 0}
        translations:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/BrandTranslationWrite'}
    UpdateBrandRequest:
      type: object
      minProperties: 1
      properties:
        image: {type: [string, 'null'], format: binary}
        banner: {type: [string, 'null'], format: binary}
        logo_url: {type: [string, 'null'], maxLength: 2048}
        website_url: {type: [string, 'null'], format: uri, maxLength: 2048}
        origin: {type: [string, 'null'], maxLength: 120}
        is_active: {type: boolean}
        sort_order: {type: integer, minimum: 0}
        translations:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/BrandTranslationWrite'}
    BrandTranslation:
      allOf:
        - {$ref: '#/components/schemas/BrandTranslationWrite'}
        - type: object
          properties:
            created_at: {type: [string, 'null'], format: date-time}
            updated_at: {type: [string, 'null'], format: date-time}
    BrandMedia:
      type: object
      required: [id, url, file_name, mime_type, size]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        url: {type: string}
        file_name: {type: string}
        mime_type: {type: [string, 'null']}
        size: {type: integer, minimum: 0}
    Brand:
      type: object
      required: [id, store_id, image, banner, logo_url, website_url, origin, is_active, sort_order, translations]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        store_id: {$ref: '#/components/schemas/PublicUlid'}
        image:
          oneOf:
            - {$ref: '#/components/schemas/BrandMedia'}
            - {type: 'null'}
        banner:
          oneOf:
            - {$ref: '#/components/schemas/BrandMedia'}
            - {type: 'null'}
        logo_url: {type: [string, 'null']}
        website_url: {type: [string, 'null'], format: uri}
        origin: {type: [string, 'null']}
        is_active: {type: boolean}
        sort_order: {type: integer, minimum: 0}
        translations: {type: array, items: {$ref: '#/components/schemas/BrandTranslation'}}
        translation_request:
          oneOf:
            - {$ref: '#/components/schemas/TranslationRequest'}
            - {type: 'null'}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    Health:
      type: object
      required: [status]
      properties:
        status: {type: string, example: ok}
        checks: {type: object, additionalProperties: {type: string}}
    RegisterRequest:
      type: object
      required: [name, email, password, password_confirmation, store_name, store_slug]
      properties:
        name: {type: string, maxLength: 255}
        email: {type: string, format: email}
        password: {type: string, format: password, minLength: 12}
        password_confirmation: {type: string, format: password}
        store_name: {type: string, maxLength: 255}
        store_slug: {type: string, pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'}
        theme_template_key: {type: string, pattern: '^[a-z0-9][a-z0-9_-]*$', default: default}
        store_country_code: {type: [string, 'null'], pattern: '^[A-Za-z]{2}$'}
        store_state: {type: [string, 'null'], maxLength: 120}
        store_city: {type: [string, 'null'], maxLength: 120}
        store_zip: {type: [string, 'null'], maxLength: 32}
        store_address_1: {type: [string, 'null'], maxLength: 255}
        store_address_2: {type: [string, 'null'], maxLength: 255}
    User:
      type: object
      required: [id, name, email, scope]
      properties:
        id: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
        name: {type: string}
        email: {type: string, format: email}
        scope: {type: string, enum: [platform, store], description: Exclusive account class}
        email_verified_at: {type: [string, 'null'], format: date-time}
    PersonalAccessToken:
      type: object
      required: [id, name, store_id, abilities, last_used_at, expires_at, created_at]
      properties:
        id: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
        name: {type: string}
        store_id: {type: [string, 'null'], pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
        abilities: {type: array, items: {type: string}}
        last_used_at: {type: [string, 'null'], format: date-time}
        expires_at: {type: [string, 'null'], format: date-time}
        created_at: {type: [string, 'null'], format: date-time}
    ManagedUser:
      allOf:
        - {$ref: '#/components/schemas/User'}
        - type: object
          required: [roles]
          properties:
            roles: {type: array, items: {type: string}}
    CreateManagedUserRequest:
      type: object
      required: [name, email, password, password_confirmation, roles]
      properties:
        name: {type: string, maxLength: 120}
        email: {type: string, format: email}
        password: {type: string, format: password, minLength: 12, writeOnly: true}
        password_confirmation: {type: string, format: password, writeOnly: true}
        roles: {type: array, minItems: 1, uniqueItems: true, items: {type: string}}
    UpdateManagedUserRequest:
      type: object
      required: [name, email, roles]
      properties:
        name: {type: string, maxLength: 120}
        email: {type: string, format: email}
        password: {type: [string, 'null'], format: password, minLength: 12, writeOnly: true}
        password_confirmation: {type: [string, 'null'], format: password, writeOnly: true}
        roles: {type: array, minItems: 1, uniqueItems: true, items: {type: string}}
    ManagedUserResponse:
      type: object
      properties:
        data: {$ref: '#/components/schemas/ManagedUser'}
    ManagedUserListResponse:
      type: object
      required: [data, links, meta]
      properties:
        data: {type: array, items: {$ref: '#/components/schemas/ManagedUser'}}
        links: {$ref: '#/components/schemas/PaginationLinks'}
        meta: {$ref: '#/components/schemas/PaginationMeta'}
    RoleListResponse:
      type: object
      properties:
        data: {type: array, items: {type: string}}
    CreateMerchantRequest:
      type: object
      required: [owner, store]
      properties:
        owner:
          type: object
          required: [name, email, password, password_confirmation]
          properties:
            name: {type: string, maxLength: 120}
            email: {type: string, format: email}
            password: {type: string, format: password, minLength: 12, writeOnly: true}
            password_confirmation: {type: string, format: password, writeOnly: true}
        store:
          type: object
          required: [name, slug]
          properties:
            name: {type: string, maxLength: 120}
            slug: {type: string, pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'}
            theme_template_key: {type: string, pattern: '^[a-z0-9][a-z0-9_-]*$', default: default}
            legal_name: {type: [string, 'null']}
            description: {type: [string, 'null']}
            email: {type: [string, 'null'], format: email}
            phone: {type: [string, 'null']}
            industry: {type: [string, 'null']}
            business_type: {type: [string, 'null'], enum: [ecommerce, b2b, services, digital, restaurant, marketplace, null]}
            currency_code: {type: string, pattern: '^[A-Za-z]{3}$'}
            language_code: {type: string}
            timezone: {type: string}
            country_code: {type: [string, 'null'], pattern: '^[A-Za-z]{2}$'}
            store_country_code: {type: [string, 'null'], pattern: '^[A-Za-z]{2}$'}
            store_state: {type: [string, 'null'], maxLength: 120}
            store_city: {type: [string, 'null'], maxLength: 120}
            store_zip: {type: [string, 'null'], maxLength: 32}
            store_address_1: {type: [string, 'null'], maxLength: 255}
            store_address_2: {type: [string, 'null'], maxLength: 255}
        roles: {type: array, minItems: 1, uniqueItems: true, items: {type: string}}
    Merchant:
      type: object
      required: [store, store_settings, owner, users]
      properties:
        store: {$ref: '#/components/schemas/Store'}
        store_settings: {$ref: '#/components/schemas/StoreSettings'}
        owner:
          oneOf:
            - {$ref: '#/components/schemas/ManagedUser'}
            - {type: 'null'}
        users: {type: array, items: {$ref: '#/components/schemas/ManagedUser'}}
    UpdateMerchantRequest:
      type: object
      required: [owner, store]
      properties:
        owner:
          type: object
          required: [name, email]
          properties:
            name: {type: string, maxLength: 120}
            email: {type: string, format: email}
            password: {type: [string, 'null'], format: password, minLength: 12, writeOnly: true}
            password_confirmation: {type: [string, 'null'], format: password, writeOnly: true}
        store:
          type: object
          required: [name, slug, status]
          properties:
            name: {type: string, maxLength: 120}
            slug: {type: string, pattern: '^[a-z0-9_-]+$'}
            legal_name: {type: [string, 'null']}
            description: {type: [string, 'null']}
            email: {type: [string, 'null'], format: email}
            phone: {type: [string, 'null']}
            industry: {type: [string, 'null']}
            business_type: {type: [string, 'null'], enum: [ecommerce, b2b, services, digital, restaurant, marketplace, null]}
            currency_code: {type: string, pattern: '^[A-Za-z]{3}$'}
            language_code: {type: string}
            timezone: {type: string}
            country_code: {type: [string, 'null'], pattern: '^[A-Za-z]{2}$'}
            store_country_code: {type: [string, 'null'], pattern: '^[A-Za-z]{2}$'}
            store_state: {type: [string, 'null'], maxLength: 120}
            store_city: {type: [string, 'null'], maxLength: 120}
            store_zip: {type: [string, 'null'], maxLength: 32}
            store_address_1: {type: [string, 'null'], maxLength: 255}
            store_address_2: {type: [string, 'null'], maxLength: 255}
            status: {type: string, enum: [draft, trial, active, suspended, frozen, closed]}
    Store:
      type: object
      description: Public Store data. Internal bigint id, plan_id, and subscription_id are never serialized.
      required: [id, name, slug, status, currency_code, language_code, timezone, is_verified, is_ai_enabled, is_pos_enabled, is_b2b_enabled, is_marketplace_enabled]
      properties:
        id: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
        name: {type: string}
        legal_name: {type: [string, 'null']}
        description: {type: [string, 'null']}
        email: {type: [string, 'null'], format: email}
        phone: {type: [string, 'null']}
        slug: {type: string}
        status: {type: string, enum: [draft, trial, active, suspended, frozen, closed]}
        primary_domain: {type: [string, 'null']}
        logo: {type: [string, 'null']}
        favicon: {type: [string, 'null']}
        cover_image: {type: [string, 'null']}
        industry: {type: [string, 'null']}
        business_type: {type: [string, 'null'], enum: [ecommerce, b2b, services, digital, restaurant, marketplace, null]}
        currency_code: {type: string, minLength: 3, maxLength: 3}
        language_code: {type: string}
        timezone: {type: string}
        country_code: {type: [string, 'null'], minLength: 2, maxLength: 2}
        is_verified: {type: boolean}
        is_ai_enabled: {type: boolean}
        is_pos_enabled: {type: boolean}
        is_b2b_enabled: {type: boolean}
        is_marketplace_enabled: {type: boolean}
        launched_at: {type: [string, 'null'], format: date-time}
        trial_ends_at: {type: [string, 'null'], format: date-time}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    PlatformStoreWriteFields:
      type: object
      description: Public Store fields only. Billing links, raw settings/metadata, preferences, owner data, and roles are prohibited.
      properties:
        name: {type: string, maxLength: 120}
        slug: {type: string, minLength: 3, maxLength: 80, pattern: '^[a-z0-9_-]+$'}
        legal_name: {type: [string, 'null'], maxLength: 255}
        description: {type: [string, 'null'], maxLength: 5000}
        email: {type: [string, 'null'], format: email}
        phone: {type: [string, 'null'], maxLength: 32}
        primary_domain: {type: [string, 'null'], maxLength: 253}
        logo: {type: [string, 'null'], maxLength: 2048}
        favicon: {type: [string, 'null'], maxLength: 2048}
        cover_image: {type: [string, 'null'], maxLength: 2048}
        industry: {type: [string, 'null'], maxLength: 120}
        business_type: {type: [string, 'null'], enum: [ecommerce, b2b, services, digital, restaurant, marketplace, null]}
        status: {type: string, enum: [draft, trial, active, suspended, frozen, closed], default: draft}
        currency_code: {type: string, pattern: '^[A-Za-z]{3}$', description: Must be an active Platform currency code.}
        language_code: {type: string, maxLength: 10, description: Must be an active Platform language locale; hyphens normalize to underscores.}
        timezone: {type: string}
        country_code: {type: [string, 'null'], pattern: '^[A-Za-z]{2}$'}
        is_verified: {type: boolean}
        is_ai_enabled: {type: boolean}
        is_pos_enabled: {type: boolean}
        is_b2b_enabled: {type: boolean}
        is_marketplace_enabled: {type: boolean}
        launched_at: {type: [string, 'null'], format: date-time}
        trial_ends_at: {type: [string, 'null'], format: date-time}
    CreatePlatformStoreRequest:
      allOf:
        - {$ref: '#/components/schemas/PlatformStoreWriteFields'}
        - type: object
          required: [name, slug]
          properties:
            locale_settings: {$ref: '#/components/schemas/CreateStoreLocaleSettings'}
    UpdatePlatformStoreRequest:
      allOf:
        - {$ref: '#/components/schemas/PlatformStoreWriteFields'}
    UpdateStoreLocaleSettingsRequest:
      type: object
      additionalProperties: false
      properties:
        currency_code: {type: string, pattern: '^[A-Za-z]{3}$', description: Must be an active Platform currency code.}
        language_code: {type: string, maxLength: 10, description: Must be an active Platform language locale; hyphens normalize to underscores.}
        timezone: {type: string, description: IANA timezone identifier.}
        country_code: {type: [string, 'null'], pattern: '^[A-Za-z]{2}$'}
        date_format: {type: string, enum: ['Y-m-d', 'd/m/Y', 'm/d/Y']}
        time_format: {type: string, enum: [12h, 24h]}
        week_starts_on: {type: string, enum: [monday, sunday, saturday]}
        weight_unit: {type: string, enum: [kg, lb]}
        dimension_unit: {type: string, enum: [cm, in]}
        decimal_places: {type: integer, minimum: 0, maximum: 4}
        decimal_separator: {type: string, enum: [dot, comma]}
        thousands_separator: {type: string, enum: [comma, dot, space, none]}
    CreateStoreLocaleSettings:
      type: object
      additionalProperties: false
      properties:
        date_format: {type: string, enum: ['Y-m-d', 'd/m/Y', 'm/d/Y']}
        time_format: {type: string, enum: [12h, 24h]}
        week_starts_on: {type: string, enum: [monday, sunday, saturday]}
        weight_unit: {type: string, enum: [kg, lb]}
        dimension_unit: {type: string, enum: [cm, in]}
        decimal_places: {type: integer, minimum: 0, maximum: 4}
        decimal_separator: {type: string, enum: [dot, comma]}
        thousands_separator: {type: string, enum: [comma, dot, space, none]}
    StoreLocaleSettings:
      type: object
      required: [store_id, currency_code, language_code, timezone, date_format, time_format, week_starts_on, weight_unit, dimension_unit, decimal_places, decimal_separator, thousands_separator, managed]
      properties:
        store_id: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
        currency_code: {type: string, minLength: 3, maxLength: 3}
        language_code: {type: string}
        timezone: {type: string}
        country_code: {type: [string, 'null'], minLength: 2, maxLength: 2}
        date_format: {type: string, enum: ['Y-m-d', 'd/m/Y', 'm/d/Y']}
        time_format: {type: string, enum: [12h, 24h]}
        week_starts_on: {type: string, enum: [monday, sunday, saturday]}
        weight_unit: {type: string, enum: [kg, lb]}
        dimension_unit: {type: string, enum: [cm, in]}
        decimal_places: {type: integer, minimum: 0, maximum: 4}
        decimal_separator: {type: string, enum: [dot, comma]}
        thousands_separator: {type: string, enum: [comma, dot, space, none]}
        managed:
          type: object
          required: [character_set, daylight_saving]
          properties:
            character_set: {type: string, enum: [UTF-8]}
            daylight_saving: {type: string, enum: [automatic]}
        updated_at: {type: [string, 'null'], format: date-time}
    StoreLocaleSettingsResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/StoreLocaleSettings'}
    StoreDomain:
      type: object
      required: [id, domain, domain_type, is_primary, status, ssl_status, is_verified]
      properties:
        id: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
        domain: {type: string, maxLength: 253}
        domain_type: {type: string, enum: [platform, custom]}
        is_primary: {type: boolean}
        status: {type: string, enum: [pending, active, failed, disabled]}
        ssl_status: {type: string, enum: [pending, provisioning, active, failed]}
        is_verified: {type: boolean}
        verified_at: {type: [string, 'null'], format: date-time}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    CreateStoreDomainRequest:
      type: object
      additionalProperties: false
      required: [domain]
      properties:
        domain: {type: string, maxLength: 253}
        domain_type: {type: string, enum: [platform, custom], default: custom}
        is_primary: {type: boolean, default: false}
        status: {type: string, enum: [pending, active, failed, disabled], default: pending}
        ssl_status: {type: string, enum: [pending, provisioning, active, failed], default: pending}
        is_verified: {type: boolean, default: false}
    UpdateStoreDomainRequest:
      type: object
      additionalProperties: false
      properties:
        domain: {type: string, maxLength: 253}
        domain_type: {type: string, enum: [platform, custom]}
        is_primary: {type: boolean}
        status: {type: string, enum: [pending, active, failed, disabled]}
        ssl_status: {type: string, enum: [pending, provisioning, active, failed]}
        is_verified: {type: boolean}
    StoreDomainResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/StoreDomain'}
    StoreDomainsResponse:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items: {$ref: '#/components/schemas/StoreDomain'}
    StoreResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/Store'}
    PlatformStoreOwner:
      type: object
      description: Public identity projected from the Store's earliest membership row.
      required: [id, name, email]
      properties:
        id: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
        name: {type: string}
        email: {type: string, format: email}
    PlatformStoreListItem:
      allOf:
        - {$ref: '#/components/schemas/Store'}
        - type: object
          required: [owner]
          properties:
            owner:
              oneOf:
                - {$ref: '#/components/schemas/PlatformStoreOwner'}
                - {type: 'null'}
    PaginatedStoreResponse:
      type: object
      required: [data, links, meta]
      properties:
        data:
          type: array
          items: {$ref: '#/components/schemas/PlatformStoreListItem'}
        links: {$ref: '#/components/schemas/PaginationLinks'}
        meta: {$ref: '#/components/schemas/PaginationMeta'}
    CreateStoreRequest:
      type: object
      required: [name, slug]
      properties:
        name: {type: string, maxLength: 120}
        slug: {type: string, pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'}
        theme_template_key: {type: string, pattern: '^[a-z0-9][a-z0-9_-]*$', default: default}
        legal_name: {type: [string, 'null'], maxLength: 255}
        description: {type: [string, 'null'], maxLength: 5000}
        email: {type: [string, 'null'], format: email}
        phone: {type: [string, 'null'], maxLength: 32}
        business_type: {type: [string, 'null'], enum: [ecommerce, b2b, services, digital, restaurant, marketplace, null]}
        currency_code: {type: string, pattern: '^[A-Za-z]{3}$', description: Must be an active Platform currency code}
        language_code: {type: string, description: Must be an active Platform language locale; hyphens normalize to underscores}
        timezone: {type: string}
        country_code: {type: [string, 'null'], pattern: '^[A-Za-z]{2}$'}
        store_country_code: {type: [string, 'null'], pattern: '^[A-Za-z]{2}$'}
        store_state: {type: [string, 'null'], maxLength: 120}
        store_city: {type: [string, 'null'], maxLength: 120}
        store_zip: {type: [string, 'null'], maxLength: 32}
        store_address_1: {type: [string, 'null'], maxLength: 255}
        store_address_2: {type: [string, 'null'], maxLength: 255}
        auto_store_translation_flag: {type: boolean, description: Opt in to future automatic Store-content translation}
        is_searchable_on_platform_flag: {type: boolean, description: Opt in to future Platform discovery/search inclusion}
        preferences: {$ref: '#/components/schemas/StorePreferences'}
    UpdateStoreProfileRequest:
      type: object
      properties:
        name: {type: string, maxLength: 120}
        slug: {type: string, pattern: '^[a-z0-9_-]+$'}
        legal_name: {type: [string, 'null'], maxLength: 255}
        description: {type: [string, 'null'], maxLength: 5000}
        email: {type: [string, 'null'], format: email}
        phone: {type: [string, 'null'], maxLength: 32}
        primary_domain: {type: [string, 'null']}
        logo: {type: [string, 'null']}
        favicon: {type: [string, 'null']}
        cover_image: {type: [string, 'null']}
        industry: {type: [string, 'null']}
        business_type: {type: [string, 'null'], enum: [ecommerce, b2b, services, digital, restaurant, marketplace, null]}
    UpdateStoreSettingsRequest:
      type: object
      properties:
        currency_code: {type: string, pattern: '^[A-Za-z]{3}$', description: Must be an active Platform currency code}
        language_code: {type: string, description: Must be an active Platform language locale; hyphens normalize to underscores}
        timezone: {type: string}
        country_code: {type: [string, 'null'], pattern: '^[A-Za-z]{2}$'}
        contact_email: {type: [string, 'null'], format: email}
        contact_phone: {type: [string, 'null'], maxLength: 32}
        store_country_code: {type: [string, 'null'], pattern: '^[A-Za-z]{2}$'}
        store_state: {type: [string, 'null'], maxLength: 120}
        store_city: {type: [string, 'null'], maxLength: 120}
        store_zip: {type: [string, 'null'], maxLength: 32}
        store_address_1: {type: [string, 'null'], maxLength: 255}
        store_address_2: {type: [string, 'null'], maxLength: 255}
        preferences: {$ref: '#/components/schemas/StorePreferences'}
    StoreSettings:
      type: object
      required: [store_id, currency_code, language_code, timezone, auto_store_translation_flag, is_searchable_on_platform_flag, preferences, capabilities]
      properties:
        store_id: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
        currency_code: {type: string, minLength: 3, maxLength: 3}
        language_code: {type: string}
        timezone: {type: string}
        country_code: {type: [string, 'null'], pattern: '^[A-Za-z]{2}$'}
        contact_email: {type: [string, 'null'], format: email}
        contact_phone: {type: [string, 'null']}
        store_country_code: {type: [string, 'null'], pattern: '^[A-Za-z]{2}$'}
        store_state: {type: [string, 'null']}
        store_city: {type: [string, 'null']}
        store_zip: {type: [string, 'null']}
        store_address_1: {type: [string, 'null']}
        store_address_2: {type: [string, 'null']}
        auto_store_translation_flag: {type: boolean}
        is_searchable_on_platform_flag: {type: boolean}
        preferences: {$ref: '#/components/schemas/StorePreferences'}
        capabilities:
          type: object
          required: [ai, pos, b2b, marketplace]
          properties:
            ai: {type: boolean}
            pos: {type: boolean}
            b2b: {type: boolean}
            marketplace: {type: boolean}
    StoreSettingsResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/StoreSettings'}
    StorePreferences:
      type: object
      additionalProperties: false
      properties:
        order_prefix: {type: string, maxLength: 12}
        date_format: {type: string, enum: ['Y-m-d', 'd/m/Y', 'm/d/Y']}
        time_format: {type: string, enum: [12h, 24h]}
        weight_unit: {type: string, enum: [kg, lb]}
        dimension_unit: {type: string, enum: [cm, in]}
        inventory_tracking: {type: boolean}
        guest_checkout: {type: boolean}
        tax_inclusive_pricing: {type: boolean}
        low_stock_threshold: {type: integer, minimum: 0, maximum: 1000000}
        support_email: {type: [string, 'null'], format: email}
    ThemePublisher:
      type: object
      required: [id, publisher_type, display_name, slug, status, support_email]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        owner_user_id:
          oneOf:
            - {$ref: '#/components/schemas/PublicUlid'}
            - {type: 'null'}
        publisher_type: {type: string, enum: [platform, third_party]}
        display_name: {type: string}
        slug: {type: string}
        status: {type: string, enum: [pending, active, suspended, rejected, closed]}
        support_email: {type: string, format: email}
        support_url: {type: [string, 'null'], format: uri}
        website_url: {type: [string, 'null'], format: uri}
        payout_account_reference: {type: [string, 'null'], description: Opaque provider reference; never credentials or bank details.}
        default_commission_bps: {type: [integer, 'null'], minimum: 0, maximum: 10000}
        verified_at: {type: [string, 'null'], format: date-time}
        terms_accepted_at: {type: [string, 'null'], format: date-time}
    ThemePublisherWriteRequest:
      type: object
      properties:
        owner_user_id:
          oneOf:
            - {$ref: '#/components/schemas/PublicUlid'}
            - {type: 'null'}
        publisher_type: {type: string, enum: [platform, third_party]}
        display_name: {type: string, maxLength: 160}
        slug: {type: string, maxLength: 100}
        status: {type: string, enum: [pending, active, suspended, rejected, closed]}
        support_email: {type: string, format: email}
        support_url: {type: [string, 'null'], format: uri}
        website_url: {type: [string, 'null'], format: uri}
        payout_account_reference: {type: [string, 'null'], maxLength: 255}
        default_commission_bps: {type: [integer, 'null'], minimum: 0, maximum: 10000}
        verified_at: {type: [string, 'null'], format: date-time}
        terms_accepted_at: {type: [string, 'null'], format: date-time}
    ThemeCategory:
      type: object
      required: [id, name, slug, category_type, sort_order, is_active]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        parent_id:
          oneOf:
            - {$ref: '#/components/schemas/PublicUlid'}
            - {type: 'null'}
        name: {type: string}
        slug: {type: string}
        description: {type: [string, 'null']}
        category_type: {type: string, enum: [industry, style, feature, catalog_size]}
        sort_order: {type: integer, minimum: 0}
        is_active: {type: boolean}
        is_primary: {type: boolean}
    ThemeCategoryWriteRequest:
      type: object
      properties:
        parent_id:
          oneOf:
            - {$ref: '#/components/schemas/PublicUlid'}
            - {type: 'null'}
        name: {type: string, maxLength: 120}
        slug: {type: string, maxLength: 120}
        description: {type: [string, 'null'], maxLength: 5000}
        category_type: {type: string, enum: [industry, style, feature, catalog_size]}
        sort_order: {type: integer, minimum: 0, maximum: 1000000}
        is_active: {type: boolean}
    ThemeVersion:
      type: object
      required: [id, version, status, engine_version, source_archive_object_key, package_sha256, package_size_bytes, uncompressed_size_bytes, file_count, manifest]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        version: {type: string}
        status: {type: string, enum: [uploaded, scanning, validating, validation_failed, ready_for_review, approved, published, deprecated, blocked]}
        engine_version: {type: string}
        minimum_platform_version: {type: [string, 'null']}
        maximum_platform_version: {type: [string, 'null']}
        source_archive_object_key: {type: string, description: Private quarantine object key; not a public download URL.}
        compiled_artifact_object_key: {type: [string, 'null']}
        package_sha256: {type: string, pattern: '^[A-Fa-f0-9]{64}$'}
        package_size_bytes: {type: integer, minimum: 1}
        uncompressed_size_bytes: {type: integer, minimum: 1}
        file_count: {type: integer, minimum: 1}
        manifest: {type: object}
        settings_schema: {type: array}
        validation_report: {type: object}
        release_notes: {type: [string, 'null']}
        approved_at: {type: [string, 'null'], format: date-time}
        published_at: {type: [string, 'null'], format: date-time}
    Theme:
      type: object
      required: [id, name, slug, summary, source_type, visibility, commercial_type, status, price, is_featured]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        publisher:
          oneOf:
            - {$ref: '#/components/schemas/ThemePublisher'}
            - {type: 'null'}
        owner_store_id:
          oneOf:
            - {$ref: '#/components/schemas/PublicUlid'}
            - {type: 'null'}
        created_by_user_id: {$ref: '#/components/schemas/PublicUlid'}
        name: {type: string}
        slug: {type: string}
        summary: {type: string}
        description: {type: [string, 'null']}
        source_type: {type: string, enum: [platform, third_party, custom]}
        visibility: {type: string, enum: [public, private, unlisted]}
        commercial_type: {type: string, enum: [free, paid, private]}
        status: {type: string, enum: [draft, pending_review, approved, published, suspended, rejected, retired]}
        price:
          type: object
          required: [amount_minor, currency_code]
          properties:
            amount_minor: {type: [integer, 'null'], minimum: 0}
            currency_code: {type: [string, 'null'], pattern: '^[A-Z]{3}$'}
        support_email: {type: [string, 'null'], format: email}
        support_url: {type: [string, 'null'], format: uri}
        documentation_url: {type: [string, 'null'], format: uri}
        demo_url: {type: [string, 'null'], format: uri}
        listing_metadata: {type: object}
        is_featured: {type: boolean}
        published_at: {type: [string, 'null'], format: date-time}
        current_version:
          oneOf:
            - {$ref: '#/components/schemas/ThemeVersion'}
            - {type: 'null'}
        versions:
          type: array
          items: {$ref: '#/components/schemas/ThemeVersion'}
        categories:
          type: array
          items: {$ref: '#/components/schemas/ThemeCategory'}
    ThemeResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/Theme'}
    ThemeWriteRequest:
      type: object
      properties:
        publisher_id:
          oneOf:
            - {$ref: '#/components/schemas/PublicUlid'}
            - {type: 'null'}
        owner_store_id:
          oneOf:
            - {$ref: '#/components/schemas/PublicUlid'}
            - {type: 'null'}
        name: {type: string, maxLength: 160}
        slug: {type: string, maxLength: 120}
        summary: {type: string, maxLength: 320}
        description: {type: [string, 'null'], maxLength: 20000}
        source_type: {type: string, enum: [platform, third_party, custom]}
        visibility: {type: string, enum: [public, private, unlisted]}
        commercial_type: {type: string, enum: [free, paid, private]}
        status: {type: string, enum: [draft, pending_review, approved, published, suspended, rejected, retired]}
        price_amount_minor: {type: [integer, 'null'], minimum: 0}
        price_currency: {type: [string, 'null'], pattern: '^[A-Z]{3}$'}
        support_email: {type: [string, 'null'], format: email}
        support_url: {type: [string, 'null'], format: uri}
        documentation_url: {type: [string, 'null'], format: uri}
        demo_url: {type: [string, 'null'], format: uri}
        listing_metadata: {type: object}
        is_featured: {type: boolean}
        category_ids:
          type: array
          maxItems: 30
          items: {$ref: '#/components/schemas/PublicUlid'}
        primary_category_id:
          oneOf:
            - {$ref: '#/components/schemas/PublicUlid'}
            - {type: 'null'}
    ThemeVersionWriteRequest:
      type: object
      required: [version, source_archive_object_key, package_sha256, package_size_bytes, uncompressed_size_bytes, file_count, manifest]
      properties:
        version: {type: string, maxLength: 30, description: Semantic version}
        engine_version: {type: string, maxLength: 30}
        minimum_platform_version: {type: [string, 'null'], maxLength: 30}
        maximum_platform_version: {type: [string, 'null'], maxLength: 30}
        source_archive_object_key: {type: string, maxLength: 2048}
        compiled_artifact_object_key: {type: [string, 'null'], maxLength: 2048}
        package_sha256: {type: string, pattern: '^[A-Fa-f0-9]{64}$'}
        package_size_bytes: {type: integer, minimum: 1, maximum: 104857600}
        uncompressed_size_bytes: {type: integer, minimum: 1, maximum: 524288000}
        file_count: {type: integer, minimum: 1, maximum: 10000}
        manifest: {type: object}
        settings_schema: {type: array}
        validation_report: {type: object}
        release_notes: {type: [string, 'null'], maxLength: 20000}
    ThemeReviewRequest:
      type: object
      required: [decision]
      properties:
        decision: {type: string, enum: [approved, changes_requested, rejected]}
        review_notes: {type: [string, 'null'], maxLength: 20000}
        rejection_codes:
          type: array
          maxItems: 100
          items: {type: string, maxLength: 120}
    ThemeLicenseIssueRequest:
      type: object
      required: [store_id, license_type]
      properties:
        store_id: {$ref: '#/components/schemas/PublicUlid'}
        license_type: {type: string, enum: [trial, free, paid, custom_owner, complimentary]}
        billing_order_item_id: {type: [integer, 'null'], minimum: 1}
        purchased_by_user_id:
          oneOf:
            - {$ref: '#/components/schemas/PublicUlid'}
            - {type: 'null'}
        trial_expires_at: {type: [string, 'null'], format: date-time}
    StoreThemeInstallRequest:
      type: object
      required: [theme_id]
      properties:
        theme_id: {$ref: '#/components/schemas/PublicUlid'}
        name: {type: [string, 'null'], maxLength: 160}
        as_trial: {type: boolean, default: false}
    StoreThemeUpdateRequest:
      type: object
      required: [customization_revision]
      properties:
        name: {type: string, maxLength: 160}
        settings_data: {type: object}
        template_data: {type: object}
        custom_css: {type: [string, 'null'], maxLength: 200000}
        customization_revision: {type: integer, minimum: 1}
    Plan:
      type: object
      required: [id, name, slug, price, status, is_featured, sort_order, features]
      properties:
        id: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
        name: {type: string}
        slug: {type: string}
        description: {type: [string, 'null']}
        best_for: {type: [string, 'null']}
        price:
          type: object
          properties:
            amount_minor: {type: [integer, 'null'], minimum: 0}
            currency_code: {type: string, pattern: '^[A-Z]{3}$'}
            billing_interval: {type: [string, 'null'], enum: [month, year, null]}
            is_custom: {type: boolean}
        status: {type: string, enum: [draft, active, archived]}
        is_featured: {type: boolean}
        sort_order: {type: integer}
        features: {type: array, items: {$ref: '#/components/schemas/PlanFeature'}}
    Feature:
      type: object
      required: [id, key, name, value_type, is_addon_eligible, is_active]
      properties:
        id: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
        key: {type: string, pattern: '^[a-z0-9]+(?:_[a-z0-9]+)*$'}
        name: {type: string}
        description: {type: [string, 'null']}
        value_type: {type: string, enum: [boolean, integer, decimal, text]}
        unit: {type: [string, 'null']}
        is_addon_eligible: {type: boolean}
        is_active: {type: boolean}
    PlanFeature:
      type: object
      required: [id, feature, is_included, is_addon, sort_order]
      properties:
        id: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
        feature: {$ref: '#/components/schemas/Feature'}
        value: {}
        is_included: {type: boolean}
        is_addon: {type: boolean}
        addon_price:
          type: [object, 'null']
          properties:
            amount_minor: {type: [integer, 'null'], minimum: 0}
            currency_code: {type: [string, 'null']}
            billing_interval: {type: [string, 'null'], enum: [month, year, null]}
        sort_order: {type: integer}
    PlanWriteRequest:
      type: object
      properties:
        name: {type: string, maxLength: 120}
        slug: {type: string}
        description: {type: [string, 'null']}
        best_for: {type: [string, 'null']}
        price_amount: {type: [integer, 'null'], minimum: 0, description: Integer minor units}
        currency_code: {type: string, pattern: '^[A-Za-z]{3}$'}
        billing_interval: {type: [string, 'null'], enum: [month, year, null]}
        is_custom_pricing: {type: boolean}
        status: {type: string, enum: [draft, active, archived]}
        is_featured: {type: boolean}
        sort_order: {type: integer, minimum: 0}
    FeatureWriteRequest:
      type: object
      properties:
        key: {type: string}
        name: {type: string}
        description: {type: [string, 'null']}
        value_type: {type: string, enum: [boolean, integer, decimal, text]}
        unit: {type: [string, 'null']}
        is_addon_eligible: {type: boolean}
        is_active: {type: boolean}
    PlanFeatureWriteRequest:
      type: object
      properties:
        value: {}
        is_included: {type: boolean}
        is_addon: {type: boolean}
        addon_price_amount: {type: [integer, 'null'], minimum: 0}
        addon_currency_code: {type: [string, 'null'], pattern: '^[A-Za-z]{3}$'}
        addon_billing_interval: {type: [string, 'null'], enum: [month, year, null]}
        sort_order: {type: integer, minimum: 0}
    Currency:
      type: object
      required: [id, name, code, symbol, symbol_position, decimal_places, usd_exchange_rate, is_base, is_active]
      properties:
        id: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
        name: {type: string, maxLength: 100}
        code: {type: string, pattern: '^[A-Z]{3}$'}
        symbol: {type: string, maxLength: 16}
        symbol_position: {type: string, enum: [before, after]}
        decimal_places: {type: integer, minimum: 0, maximum: 4}
        usd_exchange_rate:
          type: [string, 'null']
          pattern: '^[0-9]+(?:\.[0-9]{8})$'
          description: Target currency units per 1 USD; null means unconfigured.
        is_base: {type: boolean}
        is_active: {type: boolean}
        exchange_rate_updated_at: {type: [string, 'null'], format: date-time}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    CreateCurrencyRequest:
      type: object
      required: [name, code, symbol, symbol_position, decimal_places]
      properties:
        name: {type: string, maxLength: 100}
        code: {type: string, pattern: '^[A-Za-z]{3}$', description: Normalized to uppercase; USD is reserved.}
        symbol: {type: string, maxLength: 16}
        symbol_position: {type: string, enum: [before, after]}
        decimal_places: {type: integer, minimum: 0, maximum: 4}
        usd_exchange_rate:
          type: [number, 'null']
          exclusiveMinimum: 0
          maximum: 999999999999.99999999
          description: Target currency units per 1 USD.
        is_active: {type: boolean, default: true}
    UpdateCurrencyRequest:
      type: object
      minProperties: 1
      properties:
        name: {type: string, maxLength: 100}
        symbol: {type: string, maxLength: 16}
        symbol_position: {type: string, enum: [before, after]}
        decimal_places: {type: integer, minimum: 0, maximum: 4}
        usd_exchange_rate:
          type: [number, 'null']
          exclusiveMinimum: 0
          maximum: 999999999999.99999999
          description: Target currency units per 1 USD; null clears a non-base rate.
        is_active: {type: boolean}
    PageTranslationRequest:
      type: object
      required: [title, slug]
      properties:
        title: {type: string, maxLength: 250}
        slug: {type: string, maxLength: 250, description: Lowercase Unicode letters/numbers with single hyphens between segments.}
        content: {type: [string, 'null'], maxLength: 1000000}
        summary: {type: [string, 'null'], maxLength: 5000}
        seo_title: {type: [string, 'null'], maxLength: 250}
        seo_description: {type: [string, 'null'], maxLength: 1000}
        seo_keywords: {type: [string, 'null'], maxLength: 1000}
        search_keywords: {type: [string, 'null'], maxLength: 1000}
        lock_it: {type: boolean, default: false, description: Prevent automated translation writers from overwriting this record.}
    CreatePageTranslationRequest:
      allOf:
        - {$ref: '#/components/schemas/PageTranslationRequest'}
        - type: object
          required: [language_id]
          properties:
            language_id: {$ref: '#/components/schemas/PublicUlid'}
    CreatePageRequest:
      type: object
      required: [page_type, translations]
      properties:
        parent_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        page_type: {type: string, enum: [content, contact, external_link, rss]}
        sort_order: {type: integer, minimum: 0}
        layout_key: {type: [string, 'null'], maxLength: 80}
        is_homepage: {type: boolean, default: false}
        customers_only: {type: boolean, default: false}
        seo_enabled: {type: boolean, default: false}
        external_url: {type: [string, 'null'], format: uri, maxLength: 2048}
        feed_url: {type: [string, 'null'], format: uri, maxLength: 2048}
        contact_email: {type: [string, 'null'], format: email, maxLength: 320}
        contact_fields: {type: array, maxItems: 50, items: {type: object, additionalProperties: true}}
        translations: {type: array, minItems: 1, items: {$ref: '#/components/schemas/CreatePageTranslationRequest'}}
    UpdatePageRequest:
      type: object
      minProperties: 1
      properties:
        parent_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        page_type: {type: string, enum: [content, contact, external_link, rss]}
        sort_order: {type: integer, minimum: 0}
        layout_key: {type: [string, 'null'], maxLength: 80}
        is_homepage: {type: boolean}
        customers_only: {type: boolean}
        seo_enabled: {type: boolean}
        external_url: {type: [string, 'null'], format: uri, maxLength: 2048}
        feed_url: {type: [string, 'null'], format: uri, maxLength: 2048}
        contact_email: {type: [string, 'null'], format: email, maxLength: 320}
        contact_fields: {type: array, maxItems: 50, items: {type: object, additionalProperties: true}}
    PageTranslation:
      type: object
      required: [id, language, title, slug, lock_it]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        language: {$ref: '#/components/schemas/Language'}
        title: {type: string}
        slug: {type: string}
        content: {type: [string, 'null']}
        summary: {type: [string, 'null']}
        seo_title: {type: [string, 'null']}
        seo_description: {type: [string, 'null']}
        seo_keywords: {type: [string, 'null']}
        search_keywords: {type: [string, 'null']}
        lock_it: {type: boolean}
        translation_request:
          oneOf:
            - {$ref: '#/components/schemas/TranslationRequest'}
            - {type: 'null'}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    Page:
      type: object
      required: [id, page_type, status, sort_order, is_homepage, customers_only, seo_enabled, contact_fields, translations]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        store_id: {$ref: '#/components/schemas/PublicUlid'}
        parent_id: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        page_type: {type: string, enum: [content, contact, external_link, rss]}
        status: {type: string, enum: [disabled, draft, published]}
        sort_order: {type: integer, minimum: 0}
        layout_key: {type: [string, 'null']}
        is_homepage: {type: boolean}
        customers_only: {type: boolean}
        seo_enabled: {type: boolean}
        external_url: {type: [string, 'null'], format: uri}
        feed_url: {type: [string, 'null'], format: uri}
        contact_email: {type: [string, 'null'], format: email}
        contact_fields: {type: array, items: {type: object, additionalProperties: true}}
        published_at: {type: [string, 'null'], format: date-time}
        children_count: {type: integer, minimum: 0}
        created_by: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        updated_by: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        translations: {type: array, items: {$ref: '#/components/schemas/PageTranslation'}}
        translation_request:
          oneOf:
            - {$ref: '#/components/schemas/TranslationRequest'}
            - {type: 'null'}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    PolicyType:
      type: object
      required: [id, code, name, is_system, sort_order]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        code: {type: string, pattern: '^[a-z][a-z0-9_-]{0,79}$'}
        name: {type: string, maxLength: 160}
        description: {type: [string, 'null']}
        is_system: {type: boolean}
        sort_order: {type: integer, minimum: 0}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    CreatePolicyTypeRequest:
      type: object
      required: [code, name]
      properties:
        code: {type: string, pattern: '^[a-z][a-z0-9_-]{0,79}$'}
        name: {type: string, maxLength: 160}
        description: {type: [string, 'null'], maxLength: 5000}
        sort_order: {type: integer, minimum: 0}
    UpdatePolicyTypeRequest:
      type: object
      minProperties: 1
      properties:
        code: {type: string, pattern: '^[a-z][a-z0-9_-]{0,79}$'}
        name: {type: string, maxLength: 160}
        description: {type: [string, 'null'], maxLength: 5000}
        sort_order: {type: integer, minimum: 0}
    CreateStorePolicyRequest:
      type: object
      required: [policy_type_id, title]
      properties:
        policy_type_id: {$ref: '#/components/schemas/PublicUlid'}
        title: {type: string, maxLength: 255}
        slug: {type: [string, 'null'], maxLength: 160, pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'}
    UpdateStorePolicyRequest:
      type: object
      minProperties: 1
      properties:
        title: {type: string, maxLength: 255}
        slug: {type: string, maxLength: 160, pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'}
    StorePolicyTranslationRequest:
      type: object
      required: [title, content]
      properties:
        title: {type: string, maxLength: 255}
        content: {type: string, maxLength: 1000000, description: Markdown or previously sanitized HTML.}
        seo_title: {type: [string, 'null'], maxLength: 255}
        seo_description: {type: [string, 'null'], maxLength: 1000}
        lock_it: {type: boolean, default: false, description: Prevent automated translation writers from overwriting this merchant-authored record.}
    StorePolicyTranslation:
      type: object
      required: [id, language, title, content, lock_it]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        language: {$ref: '#/components/schemas/Language'}
        title: {type: string}
        content: {type: string}
        seo_title: {type: [string, 'null']}
        seo_description: {type: [string, 'null']}
        lock_it: {type: boolean}
        translation_request:
          oneOf:
            - {$ref: '#/components/schemas/TranslationRequest'}
            - {type: 'null'}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    StorePolicy:
      type: object
      required: [id, policy_type, title, slug, status, translations]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        store_id: {$ref: '#/components/schemas/PublicUlid'}
        policy_type: {$ref: '#/components/schemas/PolicyType'}
        title: {type: string}
        slug: {type: string}
        status: {type: string, enum: [disabled, draft, published]}
        published_at: {type: [string, 'null'], format: date-time}
        created_by: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        updated_by: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        translations: {type: array, items: {$ref: '#/components/schemas/StorePolicyTranslation'}}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    PolicyVersion:
      type: object
      required: [id, language, version, content]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        language: {$ref: '#/components/schemas/Language'}
        version: {type: integer, minimum: 1}
        content: {type: string}
        created_by: {oneOf: [{$ref: '#/components/schemas/PublicUlid'}, {type: 'null'}]}
        created_at: {type: [string, 'null'], format: date-time}
    StorefrontPolicy:
      type: object
      required: [id, type, title, slug, locale, content, published_at]
      properties:
        id: {$ref: '#/components/schemas/PublicUlid'}
        type:
          type: object
          required: [code, name]
          properties:
            code: {type: string}
            name: {type: string}
        title: {type: string}
        slug: {type: string}
        locale: {type: string}
        content: {type: string}
        seo_title: {type: [string, 'null']}
        seo_description: {type: [string, 'null']}
        published_at: {type: string, format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    Language:
      type: object
      required: [id, name, native_name, lang_icon, lang_image, locale, direction, is_active]
      properties:
        id: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
        name: {type: string, maxLength: 100}
        native_name: {type: string, maxLength: 100}
        lang_icon: {type: string, format: uri, description: Absolute render URL for the language country-flag icon.}
        lang_image: {type: string, format: uri, description: Absolute render URL for the language selector image.}
        locale: {type: string, maxLength: 10, pattern: '^[a-z]{2,3}(?:_[A-Z]{2})?$'}
        direction: {type: string, enum: [ltr, rtl]}
        is_active: {type: boolean}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
    StoreLanguageOption:
      type: object
      required: [id, name, native_name, lang_icon, lang_image, locale, direction, is_selected, is_default]
      properties:
        id: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
        name: {type: string}
        native_name: {type: string}
        lang_icon: {type: string, format: uri, description: Absolute render URL for the language country-flag icon.}
        lang_image: {type: string, format: uri, description: Absolute render URL for the language selector image.}
        locale: {type: string}
        direction: {type: string, enum: [ltr, rtl]}
        is_selected: {type: boolean}
        is_default: {type: boolean}
    FulfillmentTypeTranslation:
      type: object
      required: [id, locale, name, description]
      properties:
        id: {type: integer, format: int64}
        locale: {type: string, maxLength: 10}
        name: {type: string, maxLength: 255}
        description: {type: [string, 'null']}
    FulfillmentType:
      type: object
      required: [id, code, is_active, sort_order, created_at, updated_at, translations]
      properties:
        id: {type: integer, format: int64}
        code: {type: string, maxLength: 100}
        is_active: {type: boolean}
        sort_order: {type: integer, minimum: 0}
        created_at: {type: [string, 'null'], format: date-time}
        updated_at: {type: [string, 'null'], format: date-time}
        translations:
          type: array
          items: {$ref: '#/components/schemas/FulfillmentTypeTranslation'}
    FulfillmentTypesResponse:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items: {$ref: '#/components/schemas/FulfillmentType'}
    FulfillmentTypeResponse:
      type: object
      required: [data]
      properties:
        data: {$ref: '#/components/schemas/FulfillmentType'}
    FulfillmentTypeTranslationWrite:
      type: object
      required: [locale, name]
      properties:
        locale: {type: string, maxLength: 10, description: Must exist in the Language catalog.}
        name: {type: string, maxLength: 255}
        description: {type: [string, 'null']}
    CreateFulfillmentTypeRequest:
      type: object
      required: [code, translations]
      properties:
        code: {type: string, maxLength: 100, pattern: '^[a-z][a-z0-9_]*$'}
        is_active: {type: boolean, default: true}
        sort_order: {type: integer, minimum: 0, default: 0}
        translations:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/FulfillmentTypeTranslationWrite'}
    UpdateFulfillmentTypeRequest:
      type: object
      minProperties: 1
      properties:
        is_active: {type: boolean}
        sort_order: {type: integer, minimum: 0}
        translations:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/FulfillmentTypeTranslationWrite'}
    CreateLanguageRequest:
      type: object
      required: [name, native_name, locale, direction]
      properties:
        name: {type: string, maxLength: 100}
        native_name: {type: string, maxLength: 100}
        locale: {type: string, maxLength: 10}
        lang_icon: {type: string, format: uri-reference, maxLength: 2048, default: /assets/languages/flags/generic.svg}
        lang_image: {type: string, format: uri-reference, maxLength: 2048, default: /assets/languages/flags/generic.svg}
        direction: {type: string, enum: [ltr, rtl]}
        is_active: {type: boolean, default: true}
    UpdateLanguageRequest:
      type: object
      minProperties: 1
      properties:
        name: {type: string, maxLength: 100}
        native_name: {type: string, maxLength: 100}
        lang_icon: {type: string, format: uri-reference, maxLength: 2048}
        lang_image: {type: string, format: uri-reference, maxLength: 2048}
        direction: {type: string, enum: [ltr, rtl]}
        is_active: {type: boolean}
    UpdateStoreLanguagesRequest:
      type: object
      required: [language_ids, default_language_id]
      properties:
        language_ids:
          type: array
          minItems: 1
          uniqueItems: true
          items: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
        default_language_id:
          type: string
          pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'
          description: Must be included in language_ids.
    StoreLanguagesResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [store_id, default_language_id, languages]
          properties:
            store_id: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
            default_language_id: {type: [string, 'null'], pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
            languages:
              type: array
              items: {$ref: '#/components/schemas/StoreLanguageOption'}
    AccessInterfacesResponse:
      type: object
      description: Exactly one of platform_admin.available or store_admin.available can be true.
      required: [data]
      properties:
        data:
          type: object
          required: [platform_admin, store_admin]
          properties:
            platform_admin:
              type: object
              required: [interface, label, available, roles, permissions, navigation]
              properties:
                interface: {type: string, const: platform_admin}
                label: {type: string, example: Platform Admin (SaaS Owner)}
                available: {type: boolean}
                roles: {type: array, items: {type: string}}
                permissions: {type: array, items: {type: string}}
                navigation:
                  type: array
                  items:
                    type: object
                    required: [key, label, path, permission]
                    properties:
                      key: {type: string, example: platform_settings}
                      label: {type: string, example: Settings}
                      path: {type: string, example: /admin/settings}
                      permission: {type: string, example: manage platform settings}
            store_admin:
              type: object
              required: [interface, label, available, stores]
              properties:
                interface: {type: string, const: store_admin}
                label: {type: string, example: Store Admin (Merchant)}
                available: {type: boolean}
                stores:
                  type: array
                  items:
                    type: object
                    required: [id, name, slug, status, roles, permissions]
                    properties:
                      id: {type: string, pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'}
                      name: {type: string}
                      slug: {type: string}
                      status: {type: string}
                      roles: {type: array, items: {type: string}}
                      permissions: {type: array, items: {type: string}}
    DashboardSessionResponse:
      allOf:
        - {$ref: '#/components/schemas/AccessInterfacesResponse'}
        - type: object
          required: [user]
          properties:
            user: {$ref: '#/components/schemas/User'}
  responses:
    Unauthenticated:
      description: Authentication required
      content: {application/json: {schema: {type: object, properties: {message: {type: string}}}}}
    Forbidden:
      description: Forbidden
      content: {application/json: {schema: {type: object, properties: {message: {type: string}}}}}
    ValidationError:
      description: Validation failed
      content: {application/json: {schema: {type: object, properties: {message: {type: string}, errors: {type: object}}}}}
